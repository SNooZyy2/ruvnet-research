# AgentDB Integration — Section 3: Findings Registry

> Part of [AgentDB Integration Domain Analysis](analysis.md). See index for full document map.

## 3. Findings Registry

### 3a. CRITICAL Findings

| ID | Description | File(s) | Session | Status |
|----|-------------|---------|---------|--------|
| C1 | **Completely unused** — 140K+ LOC dead weight. claude-flow never calls any of 23 AgentDB controllers | All AgentDB | R8 | Open |
| C2 | **Missing WASM module** — reasoningbank_wasm.js doesn't exist, brute-force JS fallback | WASMVectorSearch.ts | R8 | Open |
| C3 | **Broken native deps** — 1,484 lines of JS fallback for broken @ruvector APIs | Multiple | R8 | Open |
| C4 | **CausalMemoryGraph statistics broken** — Wrong t-CDF formula, hardcoded tInverse=1.96. All p-values unreliable | CausalMemoryGraph.ts | R8 | Open. Confirmed R22 in TS source |
| C5 | **LearningSystem RL is cosmetic** — DQN without neural network; all 9 algorithms reduce to identical Q-value dict | LearningSystem.ts | R8 | Open. Confirmed R22 in TS source |
| C6 | **Attention MCP metrics 100% fabricated** — Math.random() for totalCalls, latencies, memory, success rates | attention-tools-handlers.ts | R16, R40 | Open |
| C7 | **Quantization benchmark crashes** — BenchmarkSuite.ts L809 interface mismatch causes runtime error | BenchmarkSuite.ts | R16 | Open |
| C8 | **QUICClient is entirely stub** — sendRequest returns hardcoded success after 100ms sleep, no QUIC protocol. R48 confirms: L108 comment admits "reference implementation" with zero network I/O, but UPGRADES to 42% due to genuine pooling/retry/batch algorithms | QUICClient.ts | R22, R48 | Open (updated) |
| C9 | **Traversal recall values hardcoded** — beam=0.948, dynamic-k=0.941, greedy=0.882 are constants not computed | traversal-optimization.ts | R22, R41 | Open |
| C10 | **Leiden clustering is no-op** — refinementPhase() does nothing beyond Louvain | clustering-analysis.ts | R22 | Open |
| C11 | **Latent-space HNSW metrics simulated** — recall=0.92+random*0.05, adaptationSpeed hardcoded to 5.5 | self-organizing-hnsw.ts | R22, R41 | Open |
| C12 | **agentdb_clear_cache tool is STUB** — Returns mock JSON message, no actual clearing | standalone-stdio.ts | R32 | Open |
| C13 | **edge-full.ts JS embedding fallback is CHARACTER HASHING** — charCodeAt-based, NOT semantic. WASM/ONNX required | edge-full.ts | R32 | Open |
| C14 | **NightlyLearner discover() returns empty** — Public API creates edges internally but always returns [] | NightlyLearner.ts | R40 | Open |
| C15 | **MCP attention encoding is hash-based** — encodeQueryVector() uses charCodeAt, not semantic embeddings | attention-tools-handlers.ts | R40 | Open |
| C16 | **Self-organizing HNSW recall fabricated** — 0.92 + Math.random()*0.05. MPC adaptation genuine but outcomes simulated | self-organizing-hnsw.ts | R41 | Open |
| C17 | **RuVectorBackend.js uses dynamic require()** — `require('ruvector')` and `require('@ruvector/core')` will fail at runtime if native packages not installed. No graceful fallback like enhanced-embeddings.ts | RuVectorBackend.js | R50 | Open |
| C18 | **AttentionService.ts "39 attention mechanisms in SQL" has ZERO backing** — `db` parameter is dead code; zero SQL operations execute in any code path. The claim of SQL-backed attention storage is completely fabricated. | AttentionService.ts | R91 | Open |
| C19 | **AttentionService.ts WASM/NAPI backends never compiled** — No `pkg/` directory, no `.node` file. All 39 claimed mechanisms default to `enabled:false`. All computation falls through to JS fallbacks. The WASM/NAPI scaffolding is inert. | AttentionService.ts | R91 | Open |
| C20 | **MemoryController.ts is the 10th disconnected persistence layer** — PURE in-memory Map store. No SQL, no file I/O, no AgentDB persistence. All stored memories are lost on process restart. Not a CRUD controller — the name and prior 95% assessment were incorrect. | MemoryController.ts | R96 | Open |
| C21 | **MemoryController.ts delete() BUG — deleted memories resurface in ANN search** — `delete()` removes the entry from the in-memory Map but does NOT call `vectorBackend.remove()`. Deleted memories remain indexed in VectorBackend and continue to appear in ANN search results. | MemoryController.ts | R96 | Open |
| C22 | **CrossAttentionController VectorBackend insert-only** — addToContext() inserts into VectorBackend but search never queries it. All stored context vectors are write-only dead weight | CrossAttentionController.ts | R114 | Open |
| C23 | **CrossAttentionController NEVER invoked from production** — computeCrossAttention() and computeMultiContextAttention() have zero callers in MemoryController.ts or any AgentDB controller. Confirms R96 H60 | CrossAttentionController.ts | R114 | Open |
| C24 | **Prime-radiant WAL commit bug** — commit_wal() does not set committed=true on WalEntry. recover_from_wal() replays ALL entries including committed ones, causing duplicate operations on crash recovery | file.rs | R108 | Open |
| C25 | **SqlJsRvfBackend O(n) brute-force search** — bruteForceSearch() iterates ALL cache entries every query with no ANN index. At MAX_PENDING_WRITES=50k vectors this is 50k comparisons per query. File header claims "SIMD-accelerated" but uses scalar 8x-unrolled JS (ILP optimization, not hardware SIMD). No performance warning emitted to caller. | SqlJsRvfBackend.ts | R115 | Open |
| C26 | **SqlJsRvfBackend .rvf format incompatibility** — Files produced by SqlJsRvfBackend are raw sql.js database exports (Uint8Array). Native RvfBackend uses @ruvector/rvf HNSW format. Both report name='rvf'. An .rvf from SqlJsRvfBackend cannot be loaded by native RvfBackend. File-format collision on shared extension. | SqlJsRvfBackend.ts | R115 | Open |
| C27 | **NativeAccelerator TensorCompress static check always fails** — `loadNativeTensorCompress` checks `gnnMod.TensorCompress?.compress` (static property) but `TensorCompress.compress` is a prototype instance method. `gnnMod.TensorCompress?.compress === undefined`, so `_tensorCompress` is never set. `tensorCompress()`, `tensorDecompress()`, and `tensorBatchCompress()` always return null despite @ruvector/gnn being installed. | NativeAccelerator.ts | R115 | Open |
| C28 | **NativeAccelerator AdamWOptimizer API mismatch — wrong call signature** — `adamWStep()` passes 7 args `(params, grads, m, v, step, lr, wd)` but actual @ruvector/attention API is `step(params: Float32Array, gradients: Float32Array): Float32Array` — only 2 args. Optimizer ignores external momentum/variance state; uses internally-managed Adam state instead. | NativeAccelerator.ts | R115 | Open |
| C29 | **NativeAccelerator InfoNceLoss API mismatch — positives type wrong and temperature missing** — `infoNceLoss()` calls `compute(anchor, positive, negatives, temperature)` — 4 args, single positive. Actual API: `compute(anchor, positives: Float32Array[], negatives: Float32Array[])` — 3 args, positives is array, temperature is constructor arg. Falls through to jsInfoNceLoss for all calls, negating native acceleration. | NativeAccelerator.ts | R115 | Open |
| C30 | **RvfBackend.ts remove() fire-and-forget** — remove() fires db.delete() with no return acknowledgment. Callers receive true unconditionally even if delete fails. Sync remove path has no data integrity guarantee. | RvfBackend.ts | R115 | Open |
| C31 | **RvfBackend.ts auto-flush silently drops queued writes** — Auto-flush triggered from sync insert() uses fire-and-forget .catch(console.error). A failed flush silently drops all queued writes with no retry and no error propagation to caller. | RvfBackend.ts | R115 | Open |
| C32 | **factory.ts SelfLearningRvfBackend never instantiated** — factory.ts only creates RuVectorBackend, RvfBackend, HNSWLibBackend, SqlJsRvfBackend. The ADR-006 self-learning stack (SelfLearningRvfBackend) is never instantiated by the factory. | factory.ts | R115 | Open |
| C33 | **factory.ts .rvf format collision** — RvfBackend writes native @ruvector/rvf binary HNSW segments; SqlJsRvfBackend writes raw SQLite WASM exports. Both use .rvf extension and both report name='rvf'. File produced by one backend cannot be loaded by the other. | factory.ts | R115 | Open |
| C34 | **SelfLearningRvfBackend contrastive projection never applied** — ContrastiveTrainer.trainBatch() is called and updates internal InfoNCE projection weights, but trainer.project() is NEVER called anywhere in SelfLearningRvfBackend. Trained projection weights have zero effect on search. | SelfLearningRvfBackend.ts | R115 | Open |
| C35 | **SelfLearningRvfBackend negative mining permanently broken** — createSamples() filters recentSearches for r.quality < negativeThreshold (0.3), but entries are only ever pushed with quality:undefined. The condition r.quality !== undefined is ALWAYS false — negative sample mining never produces any negatives, making contrastive training always empty. | SelfLearningRvfBackend.ts | R115 | Open |
| C36 | **SelfLearningRvfBackend _learningRate dead variable** — _learningRate (line 105) is updated by runAcceptanceCheck() (±10-50%) but is never passed to ContrastiveTrainer, SonaLearningBackend, or any other component. The learning rate computation is a dead variable with no observable effect. | SelfLearningRvfBackend.ts | R115 | Open |
| C37 | **FederatedSessionManager aggregate() argument type mismatch** — endSession() calls coordinator.aggregate(agent) passing the live EphemeralAgent object, but FederatedCoordinator.aggregate() expects an exportData object with {agentId, loraWeights, trajectories, quality}. Guaranteed runtime crash on any federated session end. | FederatedSessionManager.ts | R115 | Open |
| C38 | **FederatedSessionManager FedAvg not implemented** — FederatedCoordinator.aggregate() replays each trajectory sequentially via updateMasterLora() which applies per-embedding SGD (lr=0.0005*quality). No weight averaging across agents, no gradient aggregation, no model merging. This is continuous online SGD, not federated learning. | FederatedSessionManager.ts | R115 | Open |
| C39 | **SemanticQueryRouter @ruvector/router not installed** — @ruvector/router is declared in package.json (^0.1.15) but NOT installed in node_modules. SemanticQueryRouter always runs its brute-force fallback path, never the HNSW-backed native path. ADR-007 claims "Phase 1 Complete" but the primary path is permanently unavailable. | SemanticQueryRouter.ts | R115 | Open |
| C40 | **SolverBandit recordReward() never called in production** — AdaptiveIndexTuner calls selectArm() but never recordReward(). The bandit stays at Beta(1,1) uniform initialization forever, degenerating to random selection. The Thompson Sampling feedback loop is broken in production. | SolverBandit.ts | R115 | Open |
| C41 | **SolverBandit README claims 5 controller integrations; grep finds zero** — README claims SolverBandit powers SkillLibrary, ReasoningBank, NightlyLearner, TemporalCompressor, LearningSystem. Grep of entire src/ finds zero SolverBandit imports in controllers/, services/, or anywhere except AdaptiveIndexTuner (type-only) and RvfSolver.ts (comment only). | SolverBandit.ts | R115 | Open |
| C42 | **SonaLearningBackend sonaApplyBaseLora() 1-arg vs 2-arg N-API** — sonaApplyBaseLora() calls engine.applyBaseLora(Array.from(loraData)) with ONE argument, but N-API signature requires TWO: applyBaseLora(layerIdx: number, input: Array<number>). Live test confirms 1-arg call throws "Failed to convert napi value Object into rust type u32". sonaApplyBaseLora() always returns false silently via catch block. | SonaLearningBackend.ts | R115 | Open |
| C43 | **AdaptiveIndexTuner binary tier decompression non-invertible** — Binary tier decompression replaces original values with mean*1.1 or mean*0.9 regardless of magnitude. Reconstruction error is O(magnitude), not O(epsilon). decompress(compress(v)) != v for binary tier — semantic content destroyed for "frozen" embeddings. | AdaptiveIndexTuner.ts | R115 | Open |
| C44 | **AdaptiveIndexTuner Matryoshka truncation assumes MRL** — Binary tier 50% dimension reduction silently assumes embeddings were trained with Matryoshka Representation Learning. No validation that input embeddings support truncation. For standard embeddings (OpenAI, hash-based), this destroys 50% of semantic information. | AdaptiveIndexTuner.ts | R115 | Open |
| C45 | **RuvBot.ts FACADE — zero ruvector integration** — Despite header comment "RuVector's WASM vector operations" and co-location in ruvector monorepo, file imports ZERO @ruvector packages. No attention, no hyperbolic, no HNSW, no WASM. All platform integrations (Slack/Discord/Webhook) are TODO stubs. Self-training metrics (84%/12%) completely absent. | RuvBot.ts | R118 | Open |
| C46 | **rvf-mcp-server @ruvector/rvf declared but never imported** — package.json lists @ruvector/rvf as dependency but server.ts imports only McpServer and zod. All 10 MCP tools use pure in-memory JS Map. rvf_open_store creates empty state for any path (no disk read). rvf_compact is a no-op. 14th disconnected subsystem. | server.ts | R118 | Open |
| C47 | **rvf-adapters/agentdb metadata HashMap never reloaded** — RvfPatternStore.open() initializes `self.patterns = HashMap::new()` regardless of existing data. Metadata (task, reward, success, critique) written to RVF META_SEG but never read back. After restart, search_patterns() returns empty. | pattern_store.rs | R118 | Open |
| C48 | **rvf-adapters/agentdb O(n) brute-force query** — RvfVectorStore.query() delegates to RvfStore which does linear scan over all vectors. VectorStoreConfig.ef_search is threaded through everywhere but RvfStore.query() never reads it. HNSW index in index_adapter.rs is architecturally disconnected from query path. | vector_store.rs | R118 | Open |
| C49 | **ruvector CLI dist/ directory missing** — loadRuvector() requires `../dist/index.js` but dist/ does not exist in repository. All core commands (create, insert, search, stats, benchmark) crash with "Failed to load ruvector. Please run: npm run build". CLI is not self-contained as shipped. | cli.js | R118 | Open |
| C45 | **db-fallback.ts incompatible with native .rvf format** — db-fallback.ts has zero knowledge of native @ruvector/rvf binary HNSW format. If a native RvfBackend-produced .rvf file is passed to createDatabase(), it will attempt to open binary HNSW segments as SQLite, silently corrupting or discarding data. | db-fallback.ts | R115 | Open |
| C46 | **wasm-loader.ts SelfLearningRvfBackend not exported** — SelfLearningRvfBackend (ADR-006 primary integration class) is NOT exported from wasm-loader.ts, index.ts, or backends/index.ts. It exists only as an internal file in backends/rvf/ with zero public entry points — unreachable from library consumers. | wasm-loader.ts | R115 | Open |
| C50 | **AgentDBAdapter MISNAMED — zero AgentDB connection** — `AgentDBAdapter` is a plain `Map<string, MemoryEntry>` in-memory store. Despite the name, it imports ZERO AgentDB packages and has ZERO connection to any AgentDB controller, search, or persistence layer. The name creates false expectations that V3 memory uses AgentDB — it does not. 15th disconnected persistence layer. | agentdb-adapter.ts | R136 | Open |
| C51 | **AgentDBAdapter persistence placeholder** — `loadFromDisk()` and `saveToDisk()` are empty stub methods. All stored memories are lost on shutdown. The adapter is an in-memory-only store with no persistence path implemented. | agentdb-adapter.ts | R136 | Open |
| C52 | **memory-bridge.ts NOT compiled into npm dist — V3 AgentDB bridge dead at runtime** — The entire intended AgentDB bridge (1,773 LOC, 28 functions routing through ControllerRegistry to AgentDB) is excluded from the compiled npm distribution. All 28 functions return null at runtime, causing the codebase to fall back to sql.js. The quality of the source code (82-85%) is irrelevant since it never executes. | memory-bridge.ts | R136 | Open |
| C53 | **controller-registry path traversal validation no-op** — Path traversal check uses `path.resolve()` to normalize `..` sequences BEFORE comparing against the base path. Since `path.resolve()` canonicalizes the path, the `..` sequences are already removed before the check runs, making the validation ineffective against directory traversal attacks. | controller-registry.ts | R136 | Open |

### 3b. HIGH Findings

| ID | Description | File(s) | Session | Status |
|----|-------------|---------|---------|--------|
| H1 | **QUIC is TCP** — Names are misleading | QUICClient/Server | R8 | Open |
| H2 | **SIMD is fake** — Functions use only scalar 8x loop unrolling, WASM detected but unused | simd-vector-ops.ts | R8, R22 | Open |
| H3 | **ReflexionMemory breaks its paper** — Missing judge/feedback loop per arXiv:2303.11366 | ReflexionMemory.ts | R8 | Open |
| H4 | **enhanced-embeddings silent degradation** — Falls back to hash mock without warning | enhanced-embeddings.ts | R8, R22 | Open |
| H5 | **CLI is 98% real** — 35+ subcommands, 60+ methods, all fully implemented | agentdb-cli.ts | R16 | Open (Positive) |
| H6 | **34 MCP tools registered** — Complete vector DB + frontier + learning + attention surface | agentdb-mcp-server.ts | R16 | Open (Positive) |
| H7 | **ReasoningBank is canonical** — v1/v2 dual-mode, cosine similarity, SQLite + VectorBackend | ReasoningBank.ts | R16 | Open (Positive) |
| H8 | **HNSWIndex wraps real hnswlib-node** — C++ HNSW library, not reimplemented | HNSWIndex.ts | R16 | Open (Positive) |
| H9 | **HybridSearch is production-grade** — Correct BM25, three fusion strategies | HybridSearch.ts | R16 | Open (Positive) |
| H10 | **Quantization K-means++ is correct** — ADC with precomputed lookup tables | Quantization.ts | R16 | Open (Positive) |
| H11 | **Security model is solid** — Argon2id, SQL injection prevention, JWT, brute force protection | validation.ts +3 | R16 | Open (Positive) |
| H12 | **Attention mechanisms are GENUINE** — Real scaled dot-product, Xavier init, stable softmax | MultiHeadAttentionController +1 | R16 | Open (Positive) |
| H13 | **NightlyLearner DR formula incomplete** — Missing control group adjustment term | NightlyLearner.ts | R16 | Open |
| H14 | **SkillLibrary "ML" is keyword counting** — Claims ML-inspired but uses TF word counting | SkillLibrary.ts | R16 | Open |
| H15 | **Auth/token use in-memory storage** — Users, sessions, API keys, revocation all in Maps | auth.service.ts, token.service.ts | R16, R22 | Open |
| H16 | **Telemetry OTel framework not wired** — SDK init stubbed, no exporters connected | telemetry.ts | R16 | Open |
| H17 | **SyncCoordinator real but routes through stub** — Real orchestration depends on non-functional QUICClient | SyncCoordinator.ts | R22 | Open |
| H18 | **ReasoningBank O(N*M) scan** — getEmbeddingsForVectorIds uses map scan instead of reverse index | ReasoningBank.ts | R22 | Open |
| H19 | **Spectral clustering wrong** — Uses raw embeddings instead of graph Laplacian eigenvectors | clustering-analysis.ts | R22 | Open |
| H20 | **Duplicate quantization modules** — vector-quantization.ts and Quantization.ts with different approaches | agentdb | R22 | Open |
| H21 | **All 8 native controllers require EmbeddingService** — Without it, ~90% of AgentDB features non-functional | agentdb-mcp-server.js | R32 | Open |
| H22 | **BenchmarkSuite uses REAL timing** — performance.now ×28, zero fake benchmarks | BenchmarkSuite.js | R32 | Open (Positive) |
| H23 | **Cypher queries REQUIRE WASM** — edge-full.ts JS mode throws on any Cypher/SPARQL, no parser fallback | edge-full.ts | R32 | Open |
| H24 | **EnhancedAgentDBWrapper fixes R18** — Properly chains reflexionController→embedder→vectorBackend | agentdb-wrapper-enhanced.ts | R32 | Open (Positive) |
| H25 | **MultiDatabaseCoordinator sync is simulated** — Health checks hardcoded, no transactional guarantees, last-write-wins | persistence-pooled.js | R33 | Open |
| H26 | **LLMRouter has NO connection to ADR-008** — Separate priority-based system from claude-flow's 3-tier routing | LLMRouter.ts | R40 | Open |
| H27 | **NightlyLearner dead dependencies** — ReflexionMemory and SkillLibrary constructed but never called | NightlyLearner.ts | R40 | Open |
| H28 | **LLMRouter constructor timing bug** — RuvLLM can never be default provider due to async init race | LLMRouter.ts | R40 | Open |
| H29 | **Simulations NOT connected to production HNSWIndex** — All 4 latent-space files build own HNSW in TypeScript | clustering-analysis +3 | R41 | Open |
| H30 | **14 Math.random facade metrics** — Secondary metrics across latent-space simulations use baselines + noise | clustering-analysis +3 | R41 | Open |
| H31 | **Hypergraph path query trivial** — Returns [start, midpoint, end] instead of real graph traversal | hypergraph-exploration.ts | R41 | Open |
| H32 | **neural-augmentation 5 quality metrics fabricated** — edgeSelectionQuality, jointOptimizationGain, embeddingQuality, layerSkipRate, routingAccuracy all Math.random() | neural-augmentation.ts | R43 | Open |
| H33 | **neural-augmentation GNN weights random** — Initialized random, never trained. Node context uses Math.random() density/clustering | neural-augmentation.ts | R43 | Open |
| H34 | **attention.ts 9th hash-based embedding** — encodeQuery() uses character frequency hashing instead of neural embeddings. Systemic across ruvnet repos | attention.ts | R48 | Open |
| H35 | **attention.ts computeAttention facade** — Returns simulated results, never calls AgentDB core; optimizeMechanism() returns fabricated gains (predetermined multipliers) | attention.ts | R48 | Open |
| H36 | **config-manager.ts preset profiles prove simulations real** — "8.2x HNSW", "12.4% attention", "96.8% recall" are EXACT values from R35-R37 discoveries | config-manager.ts | R48 | Open (positive) |
| H37 | **health-monitor.ts LINEAR REGRESSION leak detection** — Genuine: slope via sum(x-meanX)*(y-meanY)/sum(x-meanX)² on last 10 samples, checks slope>10MB AND 80% consistent growth | health-monitor.ts | R48 | Open (positive) |
| H38 | **simulation-runner fallback mocking** — console.warn acknowledges scenarios may not exist; falls back to createMockScenario() with predetermined metrics. Infrastructure genuine, simulations may be demonstration-only | simulation-runner.ts | R48 | Open |
| H39 | **QUICClient genuine algorithms** — Exponential backoff (delay*Math.pow(2,attempt)), connection pool with timeout acquisition, batch processing with progress. Zero network I/O | QUICClient.ts | R48 | Open (positive) |
| H40 | **quic.ts full reconciliation protocol** — FullReconciliationRequest/Response with Merkle root verification, StateSummary per data type, ReconciliationReport tracking adds/updates/deletes/conflicts. JWT with 12 AuthScopes | quic.ts | R48 | Open (positive) |
| H41 | **RuVectorBackend.js GENUINE ruvector integration** — REVERSES R44 ruvector-backend.ts (12%). Real dynamic imports, VectorDB.create(), real HNSW insert/search/remove. AgentDB's OWN backend succeeds where agentic-flow's failed | RuVectorBackend.js | R50 | Open (positive) |
| H42 | **RuVectorBackend.js adaptive HNSW + production security** — Dynamically adjusts efSearch/M/efConstruction based on dataset size. Parallel batch insert with concurrency semaphore. Path validation and prototype pollution protection | RuVectorBackend.js | R50 | Open (positive) |
| H43 | **ruvector/\@ruvector/core is a runtime optional dependency** — RuVectorBackend.ts dynamically imports `ruvector` first then falls back to `@ruvector/core`; if neither is installed, all HNSW operations fail silently at runtime. Package must be separately installed. | src/backends/ruvector/index.ts, RuVectorBackend.ts | R88 | Open |
| H44 | **R20 root cause is upstream of RuVectorBackend** — Backend accepts Float32Array and performs genuine HNSW search with correct cosine/L2 distance. Failure is that EmbeddingService is never initialized in claude-flow bridge, so hash-based garbage vectors are passed in. Backend is correct; input is wrong. | src/backends/ruvector/index.ts, RuVectorBackend.ts | R88 | Open (positive) |
| H45 | **RuVectorBackend.ts comprehensive path security** — FORBIDDEN_PATH_PATTERNS blocks /etc, /proc, /sys, /dev, path traversal sequences. validatePath() enforced on every file operation. MAX_METADATA_ENTRIES=10M and MAX_VECTOR_DIMENSION=4096 guard resource limits. | RuVectorBackend.ts | R88 | Open (positive) |
| H46 | **mmap-io optional degradation lacks user warning** — RuVectorBackend.ts gracefully falls back when mmap-io is unavailable but emits no warning to operators, making memory-mapped file failures invisible in production. | RuVectorBackend.ts | R88 | Open |
| H47 | **AttentionService.ts FlashAttention tiled online-softmax is mathematically genuine** — Implements Dao et al. (2022) blocking strategy with correct online softmax (running max + log-sum-exp). Real algorithmic content, not heuristic. | AttentionService.ts | R91 | Open (positive) |
| H48 | **AttentionService.ts HyperbolicAttention genuine Poincaré ball approximation** — Uses correct Möbius addition formula for hyperbolic space projection. Consistent with attention-fallbacks.ts findings (R22). | AttentionService.ts | R91 | Open (positive) |
| H49 | **AttentionService.ts GraphRoPE and MoEAttention genuine** — GraphRoPE applies rotary positional encoding with hop distance; MoEAttention uses cosine gating with entropy regularization. Both are algorithmically sound. | AttentionService.ts | R91 | Open (positive) |
| H50 | **AttentionService.ts has 3 real downstream consumers** — NightlyLearner, CausalMemoryGraph, and ExplainableRecall actually instantiate AttentionService. Attention results flow into production AgentDB code paths (SQL path only, since WASM/NAPI absent). | AttentionService.ts | R91 | Open (positive) |
| H51 | **RuVectorBackend.ts updateAdaptiveParams() only adjusts efSearch** — Increments `indexRebuildCount` but never rebuilds or adjusts M/efConstruction. Adaptive tuning is partial — only query-time parameter changed, not index structure. | RuVectorBackend.ts | R91 | Open |
| H52 | **RuVectorBackend.ts insertBatchParallel() semaphore scope bug** — Creates a new local `Semaphore` per call, completely bypassing the instance-level semaphore. Concurrent batch inserts can exceed intended concurrency limits. | RuVectorBackend.ts | R91 | Open |
| H53 | **RuVectorBackend.ts L2 similarity uncalibrated** — Uses `Math.exp(-distance)` which maps L2 distances nonlinearly to (0, 1] without dataset-specific normalization. Results are not comparable to cosine similarity scores or across datasets with different scale. | RuVectorBackend.ts | R91 | Open |
| H54 | **AIDefenceGuard.ts behaviorBaseline is stateless across sessions** — `behaviorBaseline` is an in-memory `Map` with no persistence to AgentDB, SQLite, or any store. Behavioral baseline resets on every process restart; labeled production-ready but cannot learn patterns across restarts. | AIDefenceGuard.ts | R92 | Open |
| H55 | **AIDefenceGuard.ts enablePolicyVerification flag has zero implementation** — Config accepts `enablePolicyVerification: true` but zero policy verification code exists in the file. Setting the flag has no runtime effect whatsoever. | AIDefenceGuard.ts | R92 | Open |
| H56 | **AIDefenceGuard.ts is not wired into AgentDB runtime** — `aidefence` npm dependency (^2.1.1) is listed in ruvbot's package.json but never imported anywhere in the codebase. AIDefenceGuard.ts is entirely self-contained. The simulation scenario (`aidefence-integration.ts`) shows intent but is not executed in production. AgentDB has no runtime security guard. | AIDefenceGuard.ts, aidefence-integration.ts | R92 | Open |
| H57 | **aidefence-integration.ts scenario is non-functional** — Causal links between threats and defenses are commented out ("would link actual defense deployment to threat mitigation"). All 5 threat patterns (sql_injection, xss_attack, csrf_vulnerability, ddos_attempt, privilege_escalation) are hardcoded with fixed severity scores 0.88-0.98. EmbeddingService initialized but never used in threat analysis or defense selection. | aidefence-integration.ts | R92 | Open |
| H58 | **MemoryController.ts has NO EmbeddingService — callers must supply pre-computed embeddings** — Zero embedding generation or initialization. All store() calls require caller-supplied vectors, making MemoryController a structural encoding of the R20 root cause: whoever calls store() must have already solved the embedding problem. | MemoryController.ts | R96 | Open |
| H59 | **MemoryController.ts VectorBackend is OPTIONAL and defaults null** — When VectorBackend is null (default), search() falls back to O(n) pure-JS cosine similarity scan. The "AgentDB RESCUED" path (R50 RuVectorBackend.js) is opt-in only, not the default code path. | MemoryController.ts | R96 | Open |
| H60 | **MemoryController.ts CrossAttentionController initialized but NEVER consulted** — CrossAttentionController is instantiated and populated but is never called in search() or retrieveWithAttention(). Only SelfAttentionController and MultiHeadAttentionController are invoked. Dead integration despite full initialization. | MemoryController.ts | R96 | Open |
| H61 | **MemoryController.ts THREE attention controllers initialized regardless of enableAttention** — SelfAttentionController, CrossAttentionController, and MultiHeadAttentionController are all instantiated unconditionally in the constructor, even when `enableAttention: false`. Overhead is constant, not gated. | MemoryController.ts | R96 | Open |
| H62 | **MemoryController.ts attention score combination has unexplained /2** — The combination formula is `0.5*base + 0.5*(attentionScore/2)`, giving attention a maximum weight of 0.25 even at full strength. The division by 2 is hardcoded with no documentation or tuning rationale. | MemoryController.ts | R96 | Open |
| H63 | **MemoryController.ts confirms R48 three-layer architecture** — This is Layer 3 (JS application). Default null VectorBackend means the default call path bypasses Layer 2 (VectorBackend/HNSW entirely) and relies on O(n) scan. The three-layer fit (QUIC sync + VectorBackend + MemoryController) is confirmed but the default wiring short-circuits the vector search tier. | MemoryController.ts | R96 | Open |
| H64 | **postgres.rs store_witness() race condition** — SELECT COALESCE(MAX(sequence),0)+1 without transaction lock. Concurrent calls can assign duplicate sequence numbers | postgres.rs | R107 | Open |
| H65 | **postgres.rs find_similar() full table scan** — Fetches ALL nodes with matching dimension, computes cosine similarity in Rust iterators (no SIMD, no HNSW). O(n) worst case | postgres.rs | R107 | Open |
| H66 | **HybridStorage = FileStorage only** — mod.rs HybridStorage struct has file_storage:FileStorage but no postgres field. Despite module declaring both, only file backend is wired | mod.rs | R108 | Open |
| H67 | **memory.rs store_witness() mapping bug** — Inserts witness into self.witnesses but NEVER writes action→witness mapping into witnesses_by_action. get_witnesses_for_action() returns empty for stored witnesses | memory.rs | R108 | Open |
| H68 | **CrossAttentionController no learned weights** — No W_q, W_k, W_v projection matrices. Attention operates on raw embedding dot products only, limiting representation capacity | CrossAttentionController.ts | R114 | Open |
| H69 | **CrossAttentionController.ts math is sound** — Scaled dot-product (1/sqrt(d)), max-subtraction softmax for numerical stability, 3 aggregation strategies (average/max/weighted). The algorithms work; the integration doesn't | CrossAttentionController.ts | R114 | Open (positive) |
| H70 | **SqlJsRvfBackend insert() async flush race** — insert() enqueues to pending[] then calls this.flush().catch() fire-and-forget when batchThreshold hit. If flush fails the error is logged but writes are lost (splice already removed them). search() calls flushSync() independently creating a potential double-commit window. | SqlJsRvfBackend.ts | R115 | Open |
| H71 | **SqlJsRvfBackend getDatabase() encapsulation break** — Intentional public method exposing raw sql.js Database instance for db-fallback.ts unified mode. Callers can run arbitrary SQL on rvf_vectors table, bypassing all validation. Hard coupling from db-fallback.ts to private schema implementation detail. | SqlJsRvfBackend.ts | R115 | Open |
| H72 | **SqlJsRvfBackend rebuildCache() buffer aliasing window** — Constructs intermediate Float32Array sharing sql.js WASM heap before defensive copy. The alias window is non-zero on large result sets if sql.js reuses row buffers. | SqlJsRvfBackend.ts | R115 | Open |
| H73 | **SqlJsRvfBackend getStats() double-count bug** — Returns cache.size + pending.length. If partial flush failure leaves items in both, or INSERT OR REPLACE updates an existing id, count is inflated. No reconciliation against DB row count. | SqlJsRvfBackend.ts | R115 | Open |
| H74 | **NativeAccelerator SemanticRouter load wrapper calls static non-existent method** — `routerLoad` is set to `async (p: string) => router.SemanticRouter.load(p)` treating `load()` as a static class method. Actual API: `load()` is an instance method. `router.SemanticRouter.load(p)` is `undefined` at runtime; any `routerLoad()` call will throw. `routerSave` correctly uses instance method `r.save(p)`. | NativeAccelerator.ts | R115 | Open |
| H75 | **NativeAccelerator graphTxAvailable and graphCypherAvailable always false** — `loadGraphCapabilities` checks `GraphDatabase.prototype.beginTransaction` (not present — no transaction API in @ruvector/graph-node) and `GraphDatabase.prototype.cypher` (method is actually `.query`). Only `graphBatchInsertAvailable` can be true. Transaction and Cypher capability flags are permanently dead code. | NativeAccelerator.ts | R115 | Open |
| H76 | **NativeAccelerator @ruvector/rvf-wasm not installed** — Listed in package-lock.json v0.1.6 but no platform-specific subpackages (unlike gnn/attention which have linux-x64-gnu bins). No node_modules present. All `rvf_witness_verify`, `rvf_store_create`, `rvf_sq_load_params`, and related functions throw on import. `wasmVerifyAvailable=false` and `wasmQuantizationAvailable=false` in all deployments. | NativeAccelerator.ts | R115 | Open |
| H77 | **RvfBackend search() always throws on sync contract** — search() always throws: RvfBackend is async-only for search. This breaks the sync VectorBackend contract. Any code using RvfBackend as a sync VectorBackend will fail at runtime on search calls. | RvfBackend.ts | R115 | Open |
| H78 | **RvfBackend getStats() count permanently wrong** — getStats() returns cachedCount + pending.length, but cachedCount diverges from reality after fire-and-forget deletes or failed flushes. Count is permanently wrong until next status() call. | RvfBackend.ts | R115 | Open |
| H79 | **RvfBackend embedKernel/embedEbpf accept arbitrary binary** — embedKernel() and embedEbpf() accept raw binary Uint8Array and pass them directly to db with no size limits, content validation, or audit beyond segment storage. Arbitrary binary can be embedded in .rvf files. | RvfBackend.ts | R115 | Open |
| H80 | **RvfBackend distanceToSimilarity() inverts ip metric ranking** — distanceToSimilarity() returns -distance for ip metric. Inner product similarity is the raw dot product, not its negation. This inverts ranking for ip-metric stores, returning worst matches first. | RvfBackend.ts | R115 | Open |
| H81 | **FederatedSessionManager EWC is constant L2 not Fisher-weighted** — EWC regularization in updateMasterLora() is implemented as a constant L2 decay: penalty = ewcLambda * weight * 0.0001. Real EWC++ uses per-parameter Fisher information matrix. Neither the Fisher matrix nor parameter importance weights exist. | FederatedSessionManager.ts | R115 | Open |
| H82 | **FederatedSessionManager no differential privacy** — Header comment claims security operates on embeddings only (no user text stored). No differential privacy mechanism exists — no noise injection (Gaussian/Laplace), no epsilon-delta DP budget, no gradient clipping. | FederatedSessionManager.ts | R115 | Open |
| H83 | **SelfLearningRvfBackend recordMutationWitness() trains on unrelated puzzles** — recordMutationWitness() calls solver.train() with count:1 and a seed derived from the operation name string. RvfSolver trains on synthetic constraint-propagation puzzles (unrelated to actual embedding optimization). | SelfLearningRvfBackend.ts | R115 | Open |
| H84 | **SimdFallbacks.ts jsAdamWStep applies weight decay in wrong order** — jsAdamWStep (line 92) applies weight decay as params[i] -= lr * wd * params[i] BEFORE computing the Adam gradient correction. Standard AdamW (Loshchilov & Hutter 2019) applies decoupled weight decay AFTER the gradient step. | SimdFallbacks.ts | R115 | Open |
| H85 | **SolverBandit sampleBeta() O(1/p) latency spikes** — sampleBeta() Jöhnk branch (lines 226-233) has O(1/p) expected iterations that degrades as a+b approaches 2 from below, causing variable latency spikes in hot paths. | SolverBandit.ts | R115 | Open |
| H86 | **SolverBandit unknown-arm exploration can beat well-trained arms** — Unknown arms receive score = Math.random() + explorationBonus (0.1 default), giving range [0.1, 1.1]. This can beat a well-trained arm with alpha=100 (near-certain 0.99 success) roughly 10% of time. Exploration bonus too high for production-stability arms. | SolverBandit.ts | R115 | Open |
| H87 | **SonaLearningBackend addContext() passes JSON.stringify as contextId** — addContext() calls engine.addTrajectoryContext(trajectoryId, JSON.stringify(context)) but N-API signature is addTrajectoryContext(trajectoryId: number, contextId: string) where contextId is an opaque string key, not a serialized object. | SonaLearningBackend.ts | R115 | Open |
| H88 | **SemanticQueryRouter 12th parallel routing system** — SemanticQueryRouter routes SEARCH STRATEGY (which memory store/handler) via embedding cosine similarity. ADR-008 routes MODEL SELECTION (haiku/sonnet/opus) via complexity score. Both are "routing systems" but address completely orthogonal concerns. The proliferation is an architectural anti-pattern. | SemanticQueryRouter.ts | R115 | Open |
| H89 | **db-fallback.ts module-level singleton leak** — Module-level singleton sqlJsWrapper (line 18) means getDatabaseImplementation() returns the SAME class across all calls in a process. The SQL factory (SQL object) is cached but so is the class constructor — all databases in a process share the same initialized wasm instance. | db-fallback.ts | R115 | Open |
| H90 | **db-fallback.ts transaction() async incompatibility** — transaction() returns a wrapper function (correct for better-sqlite3 compat), but BEGIN/COMMIT/ROLLBACK use db.exec() which is synchronous in sql.js. If the wrapped fn() is async (returns Promise), COMMIT fires before the Promise resolves — the transaction is committed before the work completes. | db-fallback.ts | R115 | Open |
| H91 | **ModelCacheLoader.ts envPath check verifies directory not file** — envPath check (step 1) returns localPath=envPath when Xenova/{modelId} dir exists, BUT does NOT verify the ONNX model file is actually present (only checks the directory). Could return a localPath pointing to a directory with no ONNX file. | ModelCacheLoader.ts | R115 | Open |
| H92 | **wasm-loader.ts 9 classes exported but absent from index.ts** — wasm-loader.ts exports AgentDBSolver, ContrastiveTrainer, FederatedSessionManager, RuVectorBackend, RuVectorLearning, RvfBackend, SemanticQueryRouter, SonaLearningBackend — all absent from index.ts. Creates a two-exit-point export strategy (index.ts + wasm-loader.ts) with silent inconsistency. Callers relying on index.ts cannot access these classes. | wasm-loader.ts | R115 | Open |
| H93 | **factory.ts BackendType mismatch with detector.ts** — detector.ts exports BackendType as union of only {ruvector | hnswlib | auto} (old 2-backend world). factory.ts exports BackendType as {auto | ruvector | rvf | hnswlib}. detector.ts is imported by factory.ts, creating a type inconsistency. | factory.ts | R115 | Open |
| H94 | **RvfSolver @ruvector/rvf-solver not installed** — @ruvector/rvf-solver NOT installed in global node_modules. Package is declared in agentdb package.json as optional-by-behavior (isAvailable() guard). At research time the WASM is NOT locally runnable — all solver functionality unavailable. | RvfSolver.ts | R115 | Open |
| H95 | **claude-flow entry point does NOT initialize AgentDB at boot** — cli.js and mcp-server.js perform zero subsystem initialization (no memory, no AgentDB, no HNSW, no ruvector). AgentDB access is 3 layers deep (entry -> mcp-client.ts TOOL_REGISTRY -> agentdb-tools.ts -> memory-bridge.js). All initialization deferred to individual tool handler calls. Extends "integration gap is organizational" narrative | cli.js, mcp-server.js | R135 | Open |
| H96 | **mcp-server.js resources capability false advertisement** — Declares `capabilities.resources = {subscribe: true, listChanged: true}` in MCP server init but implements zero resource handlers, zero resource list methods, and zero subscription callbacks. Any MCP client attempting resources/list or resources/subscribe will get no response | mcp-server.js | R135 | Open |
| M-R135 | **Code duplication between cli.js and mcp-server.js MCP paths** — mcp-server.js near-duplicates the MCP bootstrap path from cli.js (same mcp-client.ts imports, same TOOL_REGISTRY, same listMCPTools/callMCPTool delegation). Two disjoint bootstrap paths share NO initialization code, creating maintenance divergence risk | cli.js, mcp-server.js | R135 | Open |
| H97 | **R20 root cause NOT fixed in V3** — AgentDBAdapter's `embeddingGenerator` is optional, and 2 of 4 factory functions in index.ts (`createMemoryService()`, `createHybridService()`) omit it entirely. The structural embedding gap from R20 persists in the V3 rewrite — callers that use these factories get an adapter with no embedding capability. | agentdb-adapter.ts, index.ts | R136 | Open |
| H98 | **Default V3 path completely bypasses real AgentDB** — `UnifiedMemoryService` is hardcoded to `AgentDBAdapter`, which is a standalone in-memory Map store with pure JS HNSW. The alternative `agentdb-backend.ts` (which has native HNSW fallback chain) and `controller-registry.ts` (~19 AgentDB controllers) are not reachable from the default service. | index.ts, agentdb-adapter.ts | R136 | Open |
| H99 | **controller-registry createEmbeddingService() fallback returns zero-filled Float32Array** — When the real embedding service is unavailable, the fallback returns `new Float32Array(dimensions)` (all zeros). Any HNSW search with zero-vector queries returns arbitrary results, silently defeating semantic search for all ~19 AgentDB controllers. | controller-registry.ts | R136 | Open |
| H100 | **controller-registry types AgentDB as `any` throughout** — The dynamically imported AgentDB module is typed as `any`, providing zero compile-time API safety. Method name typos, argument mismatches, and property access errors are invisible until runtime. | controller-registry.ts | R136 | Open |
| H101 | **4 controllers are explicit null placeholders** — `hybridSearch`, `agentMemoryScope`, `federatedSession`, and `sonaTrajectory` controllers are assigned `null` in the registry. Any code path attempting to use these controllers will hit a null reference. | controller-registry.ts | R136 | Open |
| H102 | **index.ts createHybridService misleading** — Named `createHybridService` to imply SQLite+AgentDB hybrid per ADR-009, but creates an AgentDB-only service (which itself is really a plain Map store). No SQLite backend, no hybrid composition. | index.ts | R136 | Open |
| H103 | **Two parallel module systems in @claude-flow/memory** — Flat barrel exports in index.ts coexist with orphaned DDD-style subdirectories (adapters/, backends/, services/) that are never re-exported. Consumers see only the flat surface; the structured modules are dead code. | index.ts | R136 | Open |
| H104 | **controller-registry causalRecall declared but missing from all init levels** — `causalRecall` is registered as a controller property but is not included in any of the 5 initialization levels. It is never initialized and always remains undefined. | controller-registry.ts | R136 | Open |
| H105 | **hnsw-index.ts pure JS with broken level distribution** — The default HNSW used by UnifiedMemoryService is a pure JS implementation (no hnswlib-node, no ruvector). `getRandomLevel()` uses p=0.5 instead of the standard p=1/M, producing a tree that is too tall and too sparse, degrading search recall and performance. Zero persistence — index lost on restart. | hnsw-index.ts | R136 | Open |
| C54 | **agentdb-mcp-server.ts causal graph data model broken** — causal_add_edge hardcodes fromMemoryId=0 and toMemoryId=0 regardless of actual memory references. All causal edges are disconnected from their source/target memories, making the causal graph structurally useless | agentdb-mcp-server.ts | R137 | Open |
| C55 | **agentdb-mcp-server.ts version identity mismatch** — Server constructor initializes v1.3.0, startup banner prints v2.0.0. Clients querying server version get inconsistent answers depending on which path they use | agentdb-mcp-server.ts | R137 | Open |
| C56 | **sona-agentdb-integration.ts DEAD CODE — neither dependency installed** — Imports `SonaEngine` from `@ruvector/sona` and `agentdb`, neither of which is installed in the agentic-flow workspace. All code paths are unreachable at runtime. Zero production consumers, no barrel export | sona-agentdb-integration.ts | R137 | Open |
| C57 | **sona-agentdb-integration.ts "150x-12,500x" claim is hardcoded marketing** — Line ~45 contains a hardcoded performance claim string with no backing benchmark, measurement, or test. This claim has propagated into 50+ documentation files across the project as though empirically verified | sona-agentdb-integration.ts | R137 | Open |
| C58 | **SONA MCP tool handler fabricated speedup extends C57 to live tool surface** — SONA tool handlers in v3/mcp/tools/index.ts compute `estimatedBruteForce = searchLatency * 1000` then `speedup = estimatedBruteForce / searchLatency`, always producing ~1000x "speedup" regardless of actual performance. This is not just dead code (C57) but the ACTIVE MCP tool response returned to users. Fabricated metrics in the live tool surface | v3/mcp/tools/index.ts | R138 | Open |
| C59 | **7-BUG CASCADE kills entire self-learning pipeline at runtime** (external validation, @sparkling): (1) SelfLearningRvfBackend not exported from barrel → guard false, (2) recordFeedback() on HybridBackend which lacks it, (3) memory entry IDs passed where trajectory query IDs required, (4) query path uses plain RvfBackend not searchAsync(), (5) witness chain on learningBackend not vectorBackend, (6) tick() never called → model never trains, (7) _recentSearchHits unbounded growth (memory leak). Runtime failure chain invisible to static analysis. Extends C50, confirms R20 from different angle. Source: sparkling/claude-flow-patch, ruflo #1111-#1258 | SelfLearningRvfBackend.ts + HybridBackend + controller-registry | R138 | Open |
| H106 | **agentdb-mcp-server.ts does NOT use backend factory** — Despite factory.ts existing in the same package, the MCP server uses ReflexionMemory's built-in brute-force cosine similarity for vector search. The 5-tier backend fallback chain is bypassed entirely | agentdb-mcp-server.ts | R137 | Open |
| H107 | **agentdb-mcp-server.ts EmbeddingService IS initialized** — Contradicts R20 root cause BUT only for standalone MCP server path (Pipeline 1: @xenova/transformers 384-dim). R20 root cause remains valid for claude-flow bridge path. Two divergent embedding initialization strategies coexist | agentdb-mcp-server.ts | R137 | Open |
| H108 | **factory.ts 5-tier not 4-tier backend fallback** — Chain is RuVector native > WASM > RVF SDK > HNSWLib > sql.js RVF. Prior documentation (R115) described 4 tiers. BackendType union type omits `sqljsrvf` but runtime can produce it, causing type-level inconsistency | factory.ts | R137 | Open |
| H109 | **factory.ts no memoization of detection results** — Re-probes all packages (require.resolve) on every createBackend() call. In a multi-store application, identical package detection runs redundantly | factory.ts | R137 | Open |
| H110 | **sona-agentdb-integration.ts dual-path query architecture genuine but unreachable** — AgentDB HNSW search + SONA findPatterns + weighted merge is architecturally sound. However, since neither dependency is installed, the design has never been exercised or tested | sona-agentdb-integration.ts | R137 | Open |
| H111 | **sona-agentdb-integration.ts zero export visibility** — Not exported from any barrel file (index.ts or services/index.ts). Even if dependencies were installed, no consumer could import this module | sona-agentdb-integration.ts | R137 | Open |
| H112 | **V3 MCP server zero AgentDB initialization at bootstrap** — v3/mcp/server.ts (792 LOC, 88-92%) calls getAllTools() to register all 82 MCP tools but makes zero calls to memory-initializer, AgentDB, or EmbeddingService. The MCP server boots completely standalone with no memory backend initialization. AgentDB is not just unused — it is not even started at the server level | v3/mcp/server.ts | R138 | Open |
| H113 | **Library MCP server zero AgentDB awareness across 14 methods** — v3/@claude-flow/mcp/src/server.ts (1,134 LOC, 88-92%) is the most sophisticated MCP server implementation in the project. Despite 14 methods and 9 sub-registries, it has zero memory/AgentDB/embedding references anywhere in the file or its imports. Pure protocol shell with no awareness of the vector database layer | v3/@claude-flow/mcp/src/server.ts | R138 | Open |
| H114 | **SONA tools (14) are full facades at MCP tool level** — All 14 SONA-related MCP tools in v3/mcp/tools/index.ts are facades. agentic-flow/core NOT installed, handlers fall to in-memory Maps. LoRA handlers are no-ops that return output = input.input. SONAState has zero connection to any learning backend | v3/mcp/tools/index.ts | R138 | Open |
| H115 | **SONAState zero persistence, no AgentDB, Maps only** — The SONA tool state in v3/mcp/tools/index.ts uses plain JavaScript Maps for trajectories, patterns, and metrics. All SONA state is lost on process restart. No AgentDB, no SQLite, no file I/O. This is a distinct persistence gap from the 15 previously identified disconnected subsystems | v3/mcp/tools/index.ts | R138 | Open |
| H116 | **memory-tools.ts claims ADR-006 but has no agentdb import** — The MCP memory tools module claims ADR-006 (AgentDB integration) compliance but contains zero imports from agentdb or any AgentDB-related package. It is self-contained with zod-only validation. The ADR claim in the tool documentation is false | v3/mcp/tools/index.ts (memory-tools group) | R138 | Open |
| H117 | **_recentSearchHits Map unbounded memory leak** — SelfLearningRvfBackend._recentSearchHits grows without cleanup, TTL, or size cap. In long-running agent sessions this is a memory leak. Bug #7 of the 7-bug cascade (C59). Source: sparkling/claude-flow-patch runtime analysis | SelfLearningRvfBackend.ts | R138 | Open |
| H118 | **Two competing backend factories: 2-tier vs 5-tier** — agentdb root factory.ts (ID 333, 235 LOC) has `BackendType = 'auto'\|'ruvector'\|'hnswlib'` with 2 backends. packages/agentdb factory.ts (ID 12809) has 6 BackendType options adding RVF+sql.js fallback. Both export `createBackend()` with same signature. No import resolution shows which one claude-flow actually loads | src/backends/factory.ts (agentdb root, ID 333) | R139 | Open |
