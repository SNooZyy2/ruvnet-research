## 3. Findings Registry

### 3a. CRITICAL Findings

| ID | Description | File(s) | Session | Status |
|----|-------------|---------|---------|--------|
| C1 | **Hash-based embeddings systemic** — 7+ files (Rust + JS) default to character-sum, FNV-1a, or position hash. All semantic search using defaults is character-frequency matching | ruvector-core agenticdb.rs, ruvector-cli hooks.rs, pretrain_pipeline.rs, real_trainer.rs, rlm_embedder.rs, learning-service.mjs, enhanced-embeddings.ts | C, R22, R37 | PARTIALLY SUPERSEDED by R117: onnx-embedder.ts provides real ONNX for TS async path; Rust crates remain hash-only. Sync embed() still falls through to hash (C78) |
| C2 | **HNSW deletions broken** — hnsw_rs does not support vector deletion. Delete method silently fails or panics | ruvector-core | C | Open |
| C3 | **Postgres HNSW neighbor connections empty** — `connect_node_to_neighbors()` is COMPLETELY EMPTY. Graph never actually built during insertion | index/hnsw_am.rs | R22 | Open |
| C4 | **Postgres IVFFlat mutations stubbed** — `aminsert()`, `ambulkdelete()`, `retrain()` all stubs. Index can be built/searched but not updated | index/ivfflat_am.rs | R22 | Open |
| C5 | **ruvector-graph has NO query executor** — Cypher parser generates full AST but nothing executes queries. "Working Cypher" claim is false | ruvector-graph | C | Open |
| C6 | **execute_workflow SIMULATION** — claude_integration.rs hardcodes 500 tokens, no real Claude API calls despite complete type system | claude_integration.rs | R37 | Open |
| C7 | **Speculative decoding SLOWER than vanilla** — 2K sequential forward passes for K tokens vs K vanilla passes. Current implementation is anti-optimization | speculative.rs | R35 | Open |
| C8 | **Fourth ReasoningBank with zero code sharing** — reasoning_bank.rs joins claude-flow, agentic-flow, agentdb implementations. All implement same algorithms independently | reasoning_bank.rs | R37 | Open |
| C9 | **HNSW patches FFI unsafe** — libext.rs has no bounds checking on C pointer dereferences, std::mem::forget on vectors returned to C (memory leak risk) | libext.rs | R36 | Open |
| C10 | **HNSW patches no integrity validation** — hnswio.rs has no checksum/hash on serialized dumps. Corrupted data silently loads | hnswio.rs | R36 | Open |
| C11 | **Postgres healing strategies are stubs** — All 5 healing strategies (reindex, promote, evict, block queries, repair edges) are log-only | healing/strategies.rs | R36 | Open |
| C12 | **Postgres healing metrics empty** — All 8 metric collection methods in detector.rs return empty/zero. Self-healing system cannot detect problems | healing/detector.rs | R36 | Open |
| C13 | **micro-hnsw-wasm neuromorphic unvalidated** — 6 novel features (spike encoding, homeostatic plasticity, 40Hz resonance, WTA, dendritic computation, temporal patterns) have ZERO tests | micro-hnsw-wasm | R36 | Open |
| C14 | **ruQu remote quantum providers ALL stub** — IBM/IonQ/Rigetti/Braket all return AuthenticationFailed. Only LocalSimulator works | qec_scheduler.rs | R37 | Open |
| C15 | **HNSW "patches" are vendored upstream** — hnsw.rs is unmodified hnsw_rs v0.3.3 from crates.io. Directory name `scripts/patches/` is misleading — zero ruvector-specific modifications | hnsw.rs | R52 | Open |
| C16 | **index_bench.rs HNSW is brute-force O(n)** — search_with_ef() iterates ALL nodes linearly. Benchmarks measure brute-force kNN, NOT real HNSW graph traversal | index_bench.rs | R52 | Open |
| C17 | **index_bench.rs zero postgres** — Located in ruvector-postgres/benches/ but contains zero PostgreSQL code (no pgrx, sqlx, tokio_postgres). Complete mislabeling | index_bench.rs | R52 | Open |
| C18 | **subpolynomial/mod.rs invalid paper citation** — Claims arXiv:2512.13105 (Dec 2024) but arXiv format 2512.NNNNN means Dec 2025, not 2024. Theoretical foundation suspect | subpolynomial/mod.rs | R52 | Open |
| C19 | **subpolynomial/mod.rs FALSE complexity claims** — Claims O(n^{o(1)}) subpolynomial but implements O(log n) levels × O(recourse). Same R39 false sublinearity pattern | subpolynomial/mod.rs | R52 | Open |
| C20 | **subpolynomial/mod.rs deterministic contradiction** — Claims "deterministic" subpolynomial mincut, which is an OPEN PROBLEM in graph algorithms. No randomization in code either | subpolynomial/mod.rs | R52 | Open |
| C21 | **subpoly_decoder.rs FALSE subpolynomial** — 3rd instance of false complexity. Claims "provable O(d^{2-ε} polylog d)" but ALL 3 decoders use O(n²) greedy_pair_and_correct. Zero citations, zero empirical validation | subpoly_decoder.rs | R54 | Open |
| C22 | **filters.rs COMPLETE domain mislabeling** — Named "filters" in quantum crate but implements classical coherence quality gate. Zero connection to decoder.rs. Zero quantum error filtering | filters.rs | R54 | Open |
| C23 | **ruQu contains two unrelated systems** — QEC (decoder/syndrome/surface_code) and coherence gate (filters/fabric/tile) share a crate name but have ZERO cross-references. "Qu" may mean "Quality" not "Quantum" | ruQu crate | R54 | Open |
| C24 | **"Transport-absent distributed protocol" — new pattern class.** All 5 files in ruvector-graph/src/distributed/ share the same defect: algorithm logic and state machines are correctly designed, but every network send is replaced with a debug log comment "In production, send actual network message". Zero socket I/O anywhere in the module. The distributed graph system is a design doc rendered as code | distributed/ (5 files) | R90 | Open |
| C32 | **spectral.rs fallback deflate_matrix() uses λ*I instead of rank-1 Hotelling deflation** — subtracts `lambda * Identity` instead of `lambda * v * vᵀ`. Combined with power_iteration (finds largest, not smallest eigenvalue) and deterministic init vector that may lock to trivial zero eigenvalue, the fallback `compute_eigenvalues()` is mathematically wrong for all k>1 calls. Only the `nalgebra` feature path (`#[cfg(feature = "spectral")]`) is correct | coherence/spectral.rs | R105 | Open |
| C25 | **tda.rs MISLABELED — 11th mislabeled file.** Named "Topological Data Analysis" but implements zero canonical TDA. No Vietoris-Rips complex, no boundary operators, no Betti numbers, no persistence diagrams. Implements graph metrics (connected components, clustering coefficient, diagonal-covariance degeneracy, multi-scale component counting). Misleading to any consumer expecting homology | tda.rs | R90 | Open |
| C26 | **coordinator.rs 2PC never transitions from Active.** TransactionState enum has Active/Preparing/Committed/Aborted. commit_transaction() removes the entry and logs — no prepare phase, no participant coordination, no WAL, no rollback. 2PC is type-system scaffolding only; the state machine is frozen at creation | coordinator.rs | R90 | Open |
| C27 | **rpc.rs all 4 RPC methods are stubs, gRPC feature-gated out.** RpcClient.execute_query() returns empty QueryResult with all-zero stats. RpcServer.start() logs a string. GraphRpcService and tonic::async_trait are gated behind cfg(feature="federation") absent from default Cargo.toml. In the standard build, zero gRPC code compiles | rpc.rs | R90 | Open |
| C28 | **compress.rs 12th MISLABELED FILE — "graph compression" is actually embedding/tensor quantization.** Zero GNN graph types (GraphEdge, NodeFeature, etc.) are imported or referenced. The file implements 5-tier access-frequency compression (hot/warm/cool/cold/archive) for embedding vectors and tensors. No compression of graph topology, adjacency, or edge weights whatsoever. Consumers expecting graph compression will find tensor quantization routines | compress.rs | R91 | Open |
| C29 | **compress.rs fake IEEE f16 — fixed-point *1000.0 substituted for real half-precision float.** WarmCompressor::compress_to_f16() multiplies by 1000.0 and stores as i16. This is lossy fixed-point with range ±32.767 (values ≥32.768 overflow silently). Real IEEE 754 binary16 has 5-bit exponent and full float semantics. Any consumer expecting f16 precision, exponent range, or NaN/Inf handling will get incorrect results | compress.rs | R91 | Open |
| C30 | **cuda-wasm/flash_attention.rs MISPLACED — pure CPU code in CUDA-WASM crate.** Zero cuBLAS, device memory, GPU kernels, or WASM bindgen. All computation on heap Vec<f32>. "HBM access" in docstring is conceptual, not actual. Multi-head loop is serial CPU, not GPU-parallel. Architecture mismatch between crate name and implementation | flash_attention.rs (cuda-wasm) | R92 | Open |
| C31 | **AIDefenceGuard.ts `aidefence` npm dependency declared but never imported.** package.json declares `aidefence@^2.1.1` dependency. Zero imports of this package appear in the 763-LOC file. The entire implementation is self-contained regex + heuristics. Misleading header implies external AI defense integration | AIDefenceGuard.ts | R92 | Open |
| C33 | **claude_flow_bridge.rs is purely CLI-subprocess — no Rust API, no shared memory, no IPC.** ALL integration with claude-flow goes through std::process::Command spawning npx @claude-flow/cli@latest. Every store/search/retrieve call forks a child process. This is a shell adapter, not a Rust integration. Performance: each store/search/retrieve incurs subprocess fork + IPC overhead | context/claude_flow_bridge.rs | R104 | Open |
| C34 | **claude_flow_bridge.rs skips embeddings entirely — more regressive than R20 root cause.** Unlike the ruvllm JS bridge (R20: uninitialized EmbeddingService), this Rust bridge sends raw text to CLI and parses back plain text or JSON. No HnswIndex, no vector math. Search is fully CLI-delegated: no vector similarity occurs at Rust layer | context/claude_flow_bridge.rs | R104 | Open |
| C35 | **working_memory.rs tool cache eviction is O(n) on every insert when full.** push_tool_result() calls HashMap::min_by_key() to find oldest entry — iterates entire cache on each insertion beyond capacity, making worst-case O(n*m) for m inserts. Attention weights are computed but completely ignored in eviction — the FIFO ordering contradicts the attention-salience design intent | context/working_memory.rs | R104 | Open |
| C36 | **policy.rs signature bytes NEVER VERIFIED — zero-byte placeholder activates bundles.** add_approval() checks approver ID is in allowed_approvers and is not duplicate, but never calls ANY cryptographic verification on ApprovalSignature.signature. Algorithm field (ed25519, secp256k1, placeholder) is stored but ignored. A [0u8;64] placeholder activates a policy bundle identically to a real ed25519 signature. Multi-signature governance provides ZERO cryptographic assurance | governance/policy.rs | R108 | Open |
| C37 | **policy.rs ApprovalSignature::placeholder() is production-reachable.** Public constructor injects zero-byte signatures with algorithm="placeholder". Doc says "NOT for production" but no #[cfg(test)] guard, no runtime check. Structurally indistinguishable from a backdoor | governance/policy.rs | R108 | Open |
| C38 | **storage/file.rs WAL commit flag never set — deletions non-durable.** commit_wal() never marks WalEntry.committed=true. recover_from_wal() filters !committed, so on EVERY startup ALL WAL entries replay. For DeleteEdge/DeleteNode ops: previously deleted data is silently re-inserted on restart | storage/file.rs | R108 | Open |
| C39 | **lora/adapters/trainer.rs validation loss is FAKE.** validate() computes (1-example.quality) where quality is a pre-stored field, not a model forward-pass prediction. Early stopping and best-checkpoint save are driven by this precomputed quality, never by actual model output | lora/adapters/trainer.rs | R108 | Open |
| C40 | **agent_router.rs SONA feedback critically broken — model-index semantic mismatch poisons pattern store.** record_feedback() writes agent_used (AgentType enum, 0-7 index) into SONA's model_index field (which expects 0=high-quality, 1=medium, 2=low). AgentType::Coder=0 collides with "high-quality". response_embedding is a copy of query_embedding (comment: "Simplified"). Every SONA trajectory record has wrong model quality label and identical query/response embeddings — pattern store permanently poisoned | claude_flow/agent_router.rs | R107 | Open |
| C41 | **lora/adapters/merge.rs slerp_matrix() is LINEAR interpolation (LERP), not SLERP.** Code performs standard weighted average (1-t)*a + t*b. Comment admits: "Simple linear interpolation (full SLERP requires geodesic interpolation on the weight manifold)". Any consumer relying on SLERP's manifold-preserving properties gets incorrect results | lora/adapters/merge.rs | R107 | Open |
| C42 | **cohomology/laplacian.rs power_iteration converges to eigenvalue 0 trivially** — uniform 1/sqrt(n) init IS the exact eigenvector for eigenvalue 0 of any connected graph Laplacian. Deflation removes the wrong direction, making all subsequent eigenvalue estimates unreliable. Same bug class as coherence/spectral.rs (C32). Tests pass accidentally because energy() bypasses compute_spectrum() | cohomology/laplacian.rs | R109 | Open |
| C43 | **cohomology/laplacian.rs finds LARGEST eigenvalues, not smallest (Fiedler)** — standard power iteration converges to dominant eigenvalue. Sheaf Laplacian analysis needs smallest nonzero (Fiedler value = algebraic connectivity). With num_eigenvalues=10, zero eigenvalues beyond top-10 are silently missed, undercounting Betti numbers. Combined with C42, compute_spectrum_from_matrix() is broken for all non-trivial inputs | cohomology/laplacian.rs | R109 | Open |
| C44 | **cohomology/cocycle.rs apply_adjoint() dimension indexing bug** — to compute delta^*(f) at degree n, must iterate (n+1)-simplices and distribute to n-faces. The method iterates simplices at cochain.dimension (n) and applies to (n-1)-faces — wrong source dimension. Hodge Laplacian L = delta^* delta + delta delta^* is therefore wrong. This is a SECOND broken Laplacian implementation (alongside laplacian.rs C42/C43), both independently incorrect | cohomology/cocycle.rs | R110 | Open |
| C45 | **cohomology/cocycle.rs is_coboundary() for degree>0 always returns false** — determining coboundary status requires solving delta(g)=f, but the linear system is never solved. All non-zero H^n for n>=1 is inflated: every cocycle incorrectly reports as non-coboundary, overstating topological complexity. cohomology_group.rs relies on this to separate Z^n from B^n | cohomology/cocycle.rs | R110 | Open |
| C46 | **cohomology/sheaf.rs gluing axiom semantically wrong** — is_consistent() checks rho_u(s(u)) == rho_v(s(v)) where both rho maps go FROM vertex stalks TO the edge space, but restriction maps are keyed (source,target) and (target,source), so the compared values may live in different spaces. True gluing requires a shared edge stalk domain; this collapses to identity only | cohomology/sheaf.rs | R110 | Open |
| C47 | **cohomology/sheaf.rs architecturally ISOLATED — zero consumers in cohomology pipeline** — SheafLaplacian, CohomologyGroup, and SheafNeuralLayer each build their own internal sheaf structures. No import from cohomology/sheaf.rs anywhere in the pipeline. The core Sheaf object cannot reach the cohomology computation it was designed to support | cohomology/sheaf.rs | R110 | Open |
| C48 | **cohomology/diffusion.rs update_section() clamping breaks gradient flow** — values clamped to [-max_step_change, +max_step_change] (default ±1.0) AFTER gradient step. This truncation is NOT part of dx/dt=-Lx and destroys monotone energy decrease guarantee. Creates non-conservative step that permanently biases convergence when node values exceed 1.0 | cohomology/diffusion.rs | R110 | Open |
| C49 | **cohomology/diffusion.rs diffuse_adaptive() reports converged=true at f64::MAX energy** — initializes best_energy=f64::MAX BEFORE the trial loop. If all 5 trial steps diverge, best_energy remains f64::MAX. The outer loop then sees best_energy > prev_energy and marks converged=true. Caller receives structurally valid convergence signal with f64::MAX residual obstruction — silent failure mode | cohomology/diffusion.rs | R110 | Open |
| C50 | **transport/sliced_wasserstein.rs core distance function is NOT Sliced Wasserstein** — compute_1d_ot_distance() computes pointwise |q_p - k_p|^p between individual scalar projections, not the 1D Wasserstein distance between two 1D distributions. True SW requires sorting query and key 1D projections together to compare quantiles. The correct implementation compute_distributional_ot() exists but is dead code (#[allow(dead_code)]) | transport/sliced_wasserstein.rs | R110 | Open |
| C51 | **coherence_validator.rs sigmoid inversion — gate receives wrong confidence signal on every call.** compute_confidence() returns 1/(1+exp(energy/threshold)). At energy=0 confidence=0.5; at energy=threshold confidence=0.27. Higher energy (worse coherence) → lower confidence, but function intent is that higher confidence = more certain of good result. The CoherenceGate receives an INVERTED confidence on every validation | ruvllm_integration/coherence_validator.rs | R111 | Open |
| C52 | **expander/mod.rs single-level hierarchy — O(log n) levels claimed but never built.** build_hierarchy() constructs exactly ONE level. Module documentation and papers cite multi-level decomposition as fundamental to theoretical complexity guarantees. The entire hierarchical architecture is paper-only | expander/mod.rs | R111 | Open |
| C53 | **expander/mod.rs O(n³) actual complexity vs O(m log n) claimed.** find_low_conductance_cut() tries every vertex as BFS start (O(n) × O(n) BFS = O(n²) per call), called recursively by deterministic_decompose(). Not Saranurak-Wang (which uses flow-based trimming) | expander/mod.rs | R111 | Open |
| C54 | **witness/mod.rs dead existence check (tautology).** u_exists check: `(0..self.lct.len()).any(|_| true)` is ALWAYS true for non-empty LCT. Vertex existence gates never fire. Code paths assume vertex is always present | witness/mod.rs | R111 | Open |
| C55 | **witness/mod.rs incorrect LCT cut — wrong parent severed.** delete_edge(u,v) calls lct.cut(v) with fallback to lct.cut(u). LinkCutTree::cut() severs vertex from parent, NOT an edge (u,v). The wrong parent may be cut, silently corrupting spanning tree. tree_edges and lct can permanently diverge | witness/mod.rs | R111 | Open |
| C56 | **witness/mod.rs JST SODA 2024 claim is ARCHITECTURAL FICTION.** Cites Jin-Sun-Thorup "Fully Dynamic Exact Minimum Cut in Subpolynomial Time" but implements O(n*m) BFS recompute, not subpolynomial. LCT used only as connectivity oracle. No level structure, no lazy witness propagation, no polylog updates | witness/mod.rs | R111 | Open |
| C57 | **sona_instant.rs EWC-lite is NOT EWC.** "Elastic Weight Consolidation" implementation tracks a Vec of weight indices (at most 100) by checking quality>0.7 and computing index=(quality*hidden_dim) as usize. No Fisher information matrix, no gradient accumulation, no forgetting prevention. Algorithm name is misleading | sona_instant.rs | R111 | Open |
| C59 | **sona/integration.rs dual-instance state divergence — learning never reaches inference.** SonaIntegration holds its own ewc+reasoning_bank. SonaEngine internally holds separate instances via LoopCoordinator. Background loop updates integration-local instances; inference reads engine-internal instances. EWC++ Fisher updates and pattern extraction completely siloed from inference path | sona/integration.rs | R111 | Open |
| C65 | **snn/optimizer.rs estimate_mincut() is minimum vertex degree, not a mincut algorithm.** The NeuralGraphOptimizer reward signal is computed by taking the min over graph.degree(v) as f64. This proxy equals the true mincut ONLY on regular graphs. On general graphs it over- or under-estimates arbitrarily. The RL training loop optimizes the wrong objective for all non-regular inputs | snn/optimizer.rs | R114 | Open |
| C66 | **snn/optimizer.rs decode_action() ignores all but the first spike from a 1000-neuron population.** PolicySNN fires N spikes per decision step; action is action_idx = spikes[0].neuron_id % 5. The entire SNN output collapses to a single integer, making the neural population no better than a counter. Population-coded action selection — the stated design — is absent | snn/optimizer.rs | R114 | Open |
| C67 | **snn/neuron.rs homeostatic plasticity is a dead stub — enabled by default but never applied.** step() computes d_base_thresh = rate_error * dt / tau_homeostatic but the variable is never written to self.state or self.config. The feature is silently non-functional; threshold remains frozen despite claimed stabilization | snn/neuron.rs | R114 | Open |
| C68 | **algorithm/mod.rs COMPLEXITY FRAUD — claims O(n^{o(1)}) amortized, does O(n*m) per update.** rebuild_decomposition() clones entire graph + full O(n log n) rebuild on every insert_edge/delete_edge. recompute_min_cut() runs O(n*m) BFS over all tree edges. Makes dynamic data structures (LCT, EulerTour) architecturally pointless | algorithm/mod.rs (ruvector-mincut) | R113 | Open |
| C69 | **algorithm/mod.rs cycle-edge handler EMPTY STUB.** handle_cycle_edge() does zero work on cut value when non-tree edges are inserted, making cycle edge insertions algorithmically incorrect | algorithm/mod.rs (ruvector-mincut) | R113 | Open |
| C70 | **algorithm/mod.rs LCT cut() severs parent not target.** link_cut_tree.cut(u) cuts u from parent(u), not specifically from v. If parent(u) != v, silently corrupts the link-cut tree by severing the wrong edge | algorithm/mod.rs (ruvector-mincut) | R113 | Open |
| C71 | **algorithm/mod.rs find_replacement_edge() tree-edges-only BFS.** Component discovery traverses only tree edges, excluding vertices reachable via non-tree edges. Wrong partition → may miss valid replacement edges | algorithm/mod.rs (ruvector-mincut) | R113 | Open |
| C72 | **linkcut/mod.rs is_root() conflates NodeId (u64) with internal index (usize).** Compares parent.left/right (Option<usize> index) against Some(self.id as usize) where id is u64 external ID. Only correct when IDs happen to equal insertion indices. All 13 tests use sequential IDs from 0, perfectly masking this bug | linkcut/mod.rs (ruvector-mincut) | R113 | Open |
| C73 | **linkcut/mod.rs link() semantics inverted vs docstring.** Doc says "link v as child of u" but code sets u.left=Some(v) and v.parent=Some(u), making v the parent. Callers building trees as link(child,parent) get inverted trees | linkcut/mod.rs (ruvector-mincut) | R113 | Open |
| C74 | **linkcut/mod.rs verify_root_cache() is complete stub.** Only checks cached_root < nodes.len(). Never validates against actual tree structure. Stale root cache causes find_root() to return wrong results | linkcut/mod.rs (ruvector-mincut) | R113 | Open |
| C75 | **sparql/functions.rs DefaultHasher for all 5 crypto hashes.** MD5/SHA1/SHA256/SHA384/SHA512 all use std::collections::hash_map::DefaultHasher — a non-cryptographic 64-bit hasher. All produce 16-char hex regardless of algorithm | sparql/functions.rs (ruvector-postgres) | R113 | Open |
| C76 | **sparql/functions.rs fn_replace() plain string not regex.** SPARQL 1.1 mandates XQuery regex dialect for REPLACE. Uses String::replace(). Any query with regex metacharacters produces wrong results silently | sparql/functions.rs (ruvector-postgres) | R113 | Open |
| C77 | **cognitum-gate-tilezero decision.rs zero-then-sign PermitToken panic path.** TileZero.decide() creates PermitToken with signature=[0u8;64] then passes to sign_token(). If sign_token() panics, unsigned zero-signature token could be returned | decision.rs (cognitum-gate-tilezero) | R113 | Open |
| C60 | **sona/integration.rs background loop synchronous on request thread.** trigger_background_loop() called inline in record_trajectory() on hot inference path. EWC++ Fisher updates + K-means++ clustering run synchronously with full write locks. No tokio::spawn, no thread::spawn. Large buffer stalls inference for seconds | sona/integration.rs | R111 | Open |
| C61 | **sona/integration.rs compute_pseudo_gradients() neutralizes EWC++.** update_fisher() receives query_embedding[i]*quality_score, not actual model parameter gradients. Fisher information encodes query distribution, not parameter sensitivity. Lambda regularization derived from this Fisher matrix neither identifies important parameters nor prevents catastrophic forgetting | sona/integration.rs | R111 | Open |
| C62 | **deterministic.rs coloring family STUB — core derandomization absent.** generate_coloring_family() creates correct-size Vec<EdgeColoring> but NEVER assigns colors to any edges (empty HashMaps). The (a,b)-coloring family from Lemma 3.3 of arXiv:2512.13105 is the paper's core derandomization mechanism and is completely missing | localkcut/deterministic.rs | R112 | Open |
| C63 | **deterministic.rs color assignment is arithmetic parity, NOT paper's (a,b)-family.** insert_edge() assigns colors via (u+v+forest_id)%2 for Red/Blue and (u*v)%2 for Green/Yellow. This completely replaces the paper's witness coverage guarantee with deterministic parity — no coverage property, correctness unproven | localkcut/deterministic.rs | R112 | Open |
| C64 | **ruvector-mincut lib.rs ForestPacking naming collision** — localkcut::ForestPacking shadows jtree::ForestPacking on glob imports when both features enabled. Silent wrong-type resolution at call sites | lib.rs (ruvector-mincut) | R112 | Open |
| C78 | **intelligence-engine.ts sync embed() ALWAYS falls through to hashEmbed()** — ONNX branch explicitly calls hashEmbed() with "Fallback for sync context" comment. route(), recordEpisode(), beginTrajectory(), learnFromSimilar() all use sync embed(). Only embedAsync() reaches real ONNX. Semantic intelligence claims unrealized in practice | intelligence-engine.ts | R117 | Open |
| C79 | **intelligence-engine.ts VectorDB/Map desync after import()** — recall() searches VectorDB index but looks up results in this.memories Map. After import(), memories exist in Map but NOT re-indexed into VectorDB. VectorDB returns IDs not in Map, silently dropped | intelligence-engine.ts | R117 | Open |
| C80 | **intelligence-engine.ts constructor race conditions** — initVectorDb(), initParallel(), initOnnx() all async but called from constructor without await. vectorDb/parallel/onnxEmbedder may be null when methods are called immediately after construction | intelligence-engine.ts | R117 | Open |
| C81 | **learning-engine.ts PPO ratio formula has no theoretical basis** — ratio = Math.exp(alpha * advantage) is NOT pi_new/pi_old. Clips wrong quantity. Algorithm is NOT PPO | learning-engine.ts | R117 | Open |
| C82 | **learning-engine.ts Decision Transformer is FACADE** — zero transformer attention, zero sequence modeling. Return-weighted tabular Q-learning mislabeled as DT | learning-engine.ts | R117 | Open |
| C83 | **ruvector.test.js never exercises native HNSW** — @ruvector/core absent from package.json. All 22 tests validate JS Map + cosine fallback. "Integration test" label misleading | ruvector.test.js | R117 | Open |
| C84 | **rvf-node/lib.rs verify_witness() bypasses real verify_witness_chain()** — NAPI bridge checks for non-zero terminal hash instead of calling the correct SHAKE-256 chain verifier from rvf-crypto. CLI, WASM, and claude-flow adapters ALL call verify_witness_chain() correctly. Only Node.js consumers get false security assurance about tamper-evidence. ROOT-CAUSED by R123: real verifier exists in rvf-crypto/witness.rs. One-line fix | rvf-node/src/lib.rs, rvf-crypto/src/witness.rs | R121+R123 | Open |
| C85 | **QuoteVerifier trait ZERO concrete implementations** — TEE attestation structurally incomplete. Trait defined (SGX/SEV-SNP/TDX/ARM CCA) but no `impl QuoteVerifier` exists anywhere in ruvector. Witness chain integrity works; platform verification is absent | rvf-crypto/src/attestation.rs | R124 | Open |
| C86 | **rvf-kernel is IMAGE BUILDER not micro-Linux VM** — zero KVM ioctls, no seccomp, no namespace syscalls, no hypervisor. Builds bzImage artifacts for embedding in RVF files via Docker or prebuilt. Name "micro-Linux VM" is misleading | rvf-kernel/src/lib.rs | R124 | Open |
| C87 | **rvf-kernel ZERO callers** — architecturally disconnected from runtime. Neither rvf-launch nor rvf-cli import it (Cargo.toml verified). Kernel build pipeline unreachable from main entry points | rvf-kernel/src/lib.rs | R124 | Open |
| C88 | **WasmBackend BigInt(UUID) crashes at runtime** — ingestBatch() does `BigInt(entries[i].id)` where id is a string UUID. SyntaxError at runtime. NodeBackend avoids this via numeric label allocator | npm/packages/rvf/src/backend.ts | R124 | Open |
| C89 | **WasmBackend.open()/openReadonly() throw immediately** — "WASM backend does not support file-based open (in-memory only)". Primary consumer path broken for WASM users | npm/packages/rvf/src/backend.ts | R124 | Open |
| C90 | **WasmBackend 9/20 interface methods throw** — deleteByFilter, fileId, parentId, lineageDepth, derive, embedKernel, extractKernel, embedEbpf, extractEbpf all stub. RvfBackend interface contract broken for ~45% of methods | npm/packages/rvf/src/backend.ts | R124 | Open |
| C91 | **types.ts RvfSearchResult.id string vs Rust number** — TS defines `id: string` but NAPI returns `number`. NodeBackend maps at runtime. Round-trip via delete() may break on BigInt conversion | npm/packages/rvf/src/types.ts | R124 | Open |
| C92 | **types.ts RvfStatus field names diverge from NAPI** — TS `fileSizeBytes`/`epoch` vs Rust `fileSize`/`currentEpoch`. Bridged by dual-key `??` fallback. Direct NAPI users get undefined fields | npm/packages/rvf/src/types.ts | R124 | Open |
| C93 | **ruvector-raft transport 100% stubbed** — node.rs all outbound sends are `debug!("Would send...")` with TODO comments. The crate cannot communicate across nodes. Single-process Raft simulation only | node.rs, rpc.rs | R115-cv | Open |
| C94 | **ruvector-raft + ruvector-delta-consensus ZERO integration** — no Cargo dependency in either direction. Two parallel consensus systems that never compose. 13th+ parallel subsystem | lib.rs (delta-consensus), node.rs (raft) | R115-cv | Open |
| C95 | **ruvector-replication send_to_replicas() is literal no-op** — iterates secondaries, calls `tracing::debug!()`, returns `Ok(())`. Zero network I/O. `replicate_semi_sync()` computes acks from secondary count, not actual acknowledgments | sync.rs | R115-cv | Open |
| C96 | **ruvector-replication stream.rs outputs garbage data** — hardcoded `ChangeOperation::Update` for all events, `Uuid::new_v4()` random document_id on every emission. CDC pipeline structurally correct but semantically broken | stream.rs | R115-cv | Open |
| C97 | **Raft log conflict resolution absent** — `append_entries()` rejects sequential index mismatches but never truncates follower's divergent entries per Raft §5.3. Log matching property violated — replication correctness broken | log.rs | R115-cv | Open |

### 3b. HIGH Findings

| ID | Description | File(s) | Session | Status |
|----|-------------|---------|---------|--------|
| H1 | **PQ incomplete** — codebook training partial, missing PQ distance computation | ruvector-core simd_intrinsics.rs | C | Open |
| H2 | **ID translation overhead** — u64 internal ↔ string external mapping adds indirection at every operation | ruvector-core | C | Open |
| H3 | **Attention SIMD/Rayon no-ops** — Features declared but zero actual usage across 66 files | ruvector-attention (66 files) | B | Open |
| H4 | **Core HNSW wraps hnsw_rs** — Not novel implementation, but adds real value (SIMD, storage, concurrency) | ruvector-core | C | Open |
| H5 | **FP16 path not SIMD** — matmul.rs FP16 GEMV uses scalar `half` crate, NOT NEON FP16 intrinsics despite comments claiming it | kernels/matmul.rs | R22 | Open |
| H6 | **ANE naming misleading** — Functions named `*_ane` are SCALAR FALLBACKS. BNNS API limitations acknowledged in comments | ane_ops.rs | R35 | Open |
| H7 | **Architecture-complete, persistence-incomplete** — Model backends have correct math but incomplete weight loading. Only Candle backend can load models | Gemma2, Phi3, CoreML, Mistral backends | R35 | Open |
| H8 | **TL1 kernel LUT generation wrong** — Lookup table generation has incorrect mapping but NEVER CALLED in practice | tl1_kernel.rs | R35 | Open |
| H9 | **rlm_embedder HashEmbedder FAKE** — FNV-1a hash, not semantic. NO BitNet integration | rlm_embedder.rs | R35 | Open |
| H10 | **datamap.rs use-after-free risk** — unsafe slice::from_raw_parts with mmap lifetime issues | datamap.rs | R36 | Open |
| H11 | **Postgres healing timeout not enforced** — execute_with_safeguards() does NOT enforce timeout despite comment | healing/engine.rs | R36 | Open |
| H12 | **Postgres healing bgworker registration commented out** — worker.rs register_healing_worker() has bgworker registration COMMENTED OUT | healing/worker.rs | R36 | Open |
| H13 | **prime-radiant SIMD not enabled by default** — wide::f32x8 cfg-gated behind `simd` feature which is not in default features | prime-radiant | R37 | Open |
| H14 | **Training data augmentation simplistic** — tool_dataset and claude_dataset have weak paraphrasing (5 word pairs, literal replacement) | tool_dataset.rs, claude_dataset.rs | R37 | Open |
| H15 | **tile.rs witness hash only processes 6/255 worker reports** — Significant undersampling | tile.rs | R39 | Open |
| H16 | **planner.rs CliffordT overflow** — Silently becomes u64::MAX at t_count>40 | planner.rs | R39 | Open |
| H17 | **Two independent SIMD codebases** — ruvector-core (distance metrics) and edge-net (NN inference). Zero code reuse despite overlapping math (dot product in both) | simd_intrinsics.rs, simd.rs | R52 | Open |
| H18 | **subpolynomial expander splitting incomplete** — check_and_split_expander() has TODO "A full split would require more complex logic". Just marks invalid | subpolynomial/mod.rs | R52 | Open |
| H19 | **index_bench.rs code duplication** — Reimplements HNSW (280 LOC) and IVFFlat (200 LOC) internally instead of importing ruvector-core production code | index_bench.rs | R52 | Open |
| H20 | **subpolynomial fragmentation + witness imports unused** — Two supporting modules imported but never called. Incomplete integration | subpolynomial/mod.rs | R52 | Open |
| H21 | **transpiler.rs "noise-aware" claim FALSE** — Module comment claims noise-aware transpilation but ZERO noise modeling. No error rates, gate fidelities, or noise-adaptive compilation | transpiler.rs | R54 | Open |
| H22 | **TWO independent LoRA implementations** — micro_lora.rs (training, EWC++, sona) and lora.rs (inference, quantization, WASM, edge-net). Zero code sharing despite same algorithm | micro_lora.rs, lora.rs | R54 | Open |
| H23 | **THREE independent noise/noise_model files** — ruQu noise_model.rs (R37), ruqu-core noise.rs (R54), plus surface_code.rs noise handling. No single noise source of truth | noise.rs, noise_model.rs | R54 | Open |
| H24 | **subpoly_decoder.rs zero citations** — Unlike mod.rs (invalid arXiv), subpoly_decoder.rs has NO references, DOIs, or paper citations. Theoretical claims unsupported | subpoly_decoder.rs | R54 | Open |
| H25 | **federation.rs execute_on_cluster always empty.** The scatter-gather framework (FederationStrategy: Parallel/Sequential/Fallback) dispatches tokio::spawn correctly but every work unit returns an empty stub QueryResult. No real cross-cluster data flows | federation.rs | R90 | Open |
| H26 | **gossip.rs SWIM failure detector has no network transport.** join(), send_ping(), handle_ping(), handle_ack() correctly model SWIM with incarnation numbers and suspicion timeouts, but no socket write is ever made. Failure detection cannot fire across processes; protocol only works for in-process state tracking | gossip.rs | R90 | Open |
| H27 | **H1 RESOLVED — PQ now complete in product_quantization.rs.** H1 ("PQ incomplete — codebook training partial, missing PQ distance computation") was recorded against simd_intrinsics.rs. R90 confirms product_quantization.rs (88-92%) has complete k-means++ codebook training, encode(), and ADC LookupTable. The capability exists; it is in a different module than H1 assumed | product_quantization.rs | R90 | Resolved |
| H28 | **kv_cache/legacy.rs per-head scale stomping bug.** In the 4-bit quantization path, the per-head min and max scale values are recomputed and overwritten on every new-token append. Prior scale values are discarded, making it impossible to correctly dequantize previously quantized tokens after even one update. Dequantization of the full KV history is silently corrupted | kv_cache/legacy.rs | R91 | Open |
| H29 | **kv_cache/legacy.rs no eviction policy.** The KV cache grows unboundedly with no LRU, sliding window, or capacity limit. For long-context inference this will exhaust memory without graceful degradation. Contrast with ruvllm/kv_cache.rs which has a hot/cold tiering strategy | kv_cache/legacy.rs | R91 | Open |
| H30 | **mmap.rs pin count allocated but unused — no page eviction mechanism.** PinnedPage and pin_count fields are defined but pin_count is never incremented, checked, or used in any eviction guard. The mmap region is always available for unmapping regardless of whether callers have outstanding references | mmap.rs | R91 | Open |
| H31 | **speculative.rs sequential path verification negates tree parallelism benefit.** EAGLE tree decoding produces a draft tree of candidate continuations intended for parallel verification. This implementation verifies accepted tokens sequentially along the accepted path only. No batch forward pass over tree branches occurs. The novelty is in tree construction (lambda-guided confidence) but the parallel verification that makes EAGLE fast is not implemented | speculative.rs | R91 | Open |
| H32 | **compress.rs PQ codebook uses trivial linear interpolation, not k-means.** ColdCompressor::encode_pq() selects centroids by linear interpolation between min and max values (linspace). This produces uniform quantization, NOT vector quantization optimized to data distribution. Product quantization requires k-means or k-means++ training on representative data. The "PQ" labeling is misleading — this is uniform scalar quantization applied per subvector dimension | compress.rs | R91 | Open |
| H33 | **AIDefenceGuard.ts behavioral baseline in-memory only.** analyzeBehavior() computes 4 statistical features (length, punct, caps, digits) with hardcoded 2.0 deviation threshold. Baseline stored in ephemeral Map, lost on process restart. No persistence to AgentDB, DB, or any storage. Trivially gameable — not actual ML anomaly detection | AIDefenceGuard.ts | R92 | Open |
| H34 | **AIDefenceGuard.ts enablePolicyVerification ghost feature.** Valid config flag but zero policy verification logic exists in the file. Flag checked nowhere in analyze(). Config-level ghost — consumers enabling this option get zero additional protection | AIDefenceGuard.ts | R92 | Open |
| H35 | **lorentz_cascade.rs inference-only — no learning mechanism.** Curvatures and focal directions are fixed at initialization, never updated. head.weight is uniform. For a claimed "novel architecture," absence of backpropagation through hyperbolic operations limits to inference use only | lorentz_cascade.rs | R92 | Open |
| H36 | **hyperbolic-hnsw shard.rs ID mapping O(n²) search.** ShardedHyperbolicHnsw.search() uses linear scan over id_to_shard for every result per shard — O(|global_ids| × |shards| × k). Inefficient for large datasets; needs bidirectional map | shard.rs (hyperbolic-hnsw) | R92 | Open |
| H37 | **spike_driven.rs saturating_mul contradicts "multiplication-free" claim.** spike_value_contribution() uses saturating_mul — a multiplication — directly contradicting the neuromorphic energy advantage (Yao et al. 2023) | spike_driven.rs | R93 | Open |
| H38 | **spike_driven.rs energy_ratio hardcoded 30% average spike rate.** Reported 87.2x energy savings is a constant, not measured from actual spike density | spike_driven.rs | R93 | Open |
| H39 | **sparse_attention.rs partition boundaries are PLACEHOLDER.** estimate_partition_boundaries() uses uniform stride with comment "in practice would come from actual mincut partition info." The core value proposition (mincut-aware sparsity) does not hold at runtime | sparse_attention.rs | R93 | Open |
| H40 | **sparse_attention.rs _target_density and _gate dead params.** Adaptive density scheduling (3 strategies: Linear/Threshold/Adaptive) computes density but build_sparse_positions() ignores it. Window hardcoded at 4 | sparse_attention.rs | R93 | Open |
| H41 | **q15.rs u16/i16 type incompatibility with rope.rs.** q15.rs wraps u16 (unsigned), rope.rs uses i16 (signed for rotation). Same Q15 scale but divergent types — rope.rs cannot use Q15::from_f32() without sign loss | q15.rs, rope.rs | R93 | Open |
| H42 | **q15.rs newtype exported but UNUSED within crate.** rope.rs duplicates the scale constant as a raw const. Type-safe Q15 exists but nobody imports it | q15.rs | R93 | Open |
| H43 | **qgemm.rs compile-time-only SIMD dispatch.** No is_x86_feature_detected!() runtime guard. Generic cargo build without -C target-feature=+avx2 silently uses scalar on AVX2 machines | qgemm.rs | R93 | Open |
| H44 | **manager.rs no selective eviction.** Pure FIFO graduation (oldest token always moves to next tier). No H2O, attention-sink, StreamingLLM, or any attention-weighted eviction. Semantically critical mid-sequence tokens evicted identically to noise | manager.rs | R93 | Open |
| H45 | **manager.rs SQuat and KVQuant quantizers dead code.** Enum variants documented and used in memory estimates but squat_quantizer/kvquant_quantizer fields always None. Archive tier hardcoded to KIVI 2-bit regardless of config | manager.rs | R93 | Open |
| H46 | **manager.rs rematerialization wired but never triggered.** policy.rs RematerializationPolicy is complete and correct, but append()/attention() never call should_rematerialize() | manager.rs | R93 | Open |
| H47 | **ffn.rs single-scale approximation.** forward() uses w1_scales[0] for entire activation scale. Per-row scales from qgemm_i8 are ignored — incorrect for rows with significantly different scale values | ffn.rs | R93 | Open |
| H48 | **kvquant.rs 3-bit packing silently broken.** bits=3 branch in all three quantization paths falls back to 4-bit packing. Paper's "5.3x compression vs FP16" requires genuine 3-bit. Test checks formula, not actual bit density | kvquant.rs | R93 | Open |
| H49 | **early_exit.rs abs() overflow risk.** lambda_delta_abs computed as gate.lambda_delta().abs() as u32. i32::MIN.abs() panics in debug or wraps in release. Pattern duplicated in evaluate_exit_conditions() | early_exit.rs | R93 | Open |
| H50 | **early_exit.rs unnormalized logit verification.** verify_speculation() compares argmax of raw i32 logits without softmax. Draft token can pass even when full model's distribution is radically different | early_exit.rs | R93 | Open |
| H51 | **early_exit.rs adaptive/config threshold inconsistency.** calculate_adaptive_exit_layer() uses hardcoded 30000/25000 thresholds that differ from config.min_lambda_stability_q15 (28000 default). Can select earlier layer then refuse to exit there | early_exit.rs | R93 | Open |
| H52 | **mod_routing.rs route_unstable_tokens / route_stable_tokens byte-identical.** Both functions iterate left-to-right assigning Compute until target_count reached. Semantic distinction is managed by upstream capacity calc only. Divergence risk if one is modified | mod_routing.rs | R94 | Open |
| H53 | **mod_routing.rs boundary detection is stride heuristic, not actual mincut edges.** mark_boundary_tokens() uses regular stride intervals. Comment acknowledges "in practice would use actual boundary edge IDs from mincut" but GatePacket only carries aggregate stats, not per-token boundary membership | mod_routing.rs | R94 | Open |
| H54 | **quantized_store.rs per-layer counter vs per-head data.** warm_len[layer] is single counter shared across all heads. graduate_to_archive() assumes all heads at identical fill levels. Uneven head pushing corrupts per-head data by shifting misaligned byte ranges | quantized_store.rs | R94 | Open |
| H55 | **quant4.rs fully scalar, no SIMD in kernel/ directory.** In same directory as qgemm.rs (real AVX2+NEON), this file has zero SIMD intrinsics. Conspicuous quality gap | quant4.rs | R94 | Open |
| H56 | **quant4.rs BlockInt4Weights write-only.** No dequantize_row, no GEMV/GEMM method. Cannot be used for inference, only construction | quant4.rs | R94 | Open |
| H57 | **state.rs unchecked layer index — UB risk.** k_cache()/v_cache() use unbounded [start..] slice with no layer bounds check. Caller passing OOB layer_idx triggers undefined behavior | state.rs | R94 | Open |
| H58 | **state.rs no serialization support.** Zero Serialize/Deserialize derives, no safetensors, no save/load. State exists only for single inference session lifetime. No version tagging or migration path | state.rs | R94 | Open |
| H59 | **metrics.rs docstring claims "cache hit rates per tier" but file has zero hit-rate tracking.** QualityTracker tracks PPL/accuracy quality scores only. The "metrics" name and module doc are misleading | metrics.rs | R94 | Open |
| H60 | **replay.rs false KL divergence claim.** detect_distribution_shift() documents "Kullback-Leibler divergence approximation" but computes per-dimension standardized mean difference (Cohen's d analogue). Ignores variance of recent window entirely | replay.rs | R94 | Open |
| H60a | **arena.rs aliasing UB hazard.** alloc_i8/alloc_f32/alloc_i32 return &mut [T] slices derived from the SAME backing Vec<u8>. Rust borrow checker cannot track these across call sites — callers holding two live mutable slices simultaneously trigger undefined behavior. No lifetime coupling prevents concurrent mutable borrows of overlapping regions | arena.rs | R96 | Open |
| H60b | **squat.rs calibrate() STUB — ignores calibration_data entirely.** Real SQuAt paper uses PCA/SVD on actual KV distributions to learn the orthogonal basis. This implementation initializes a Hadamard basis then runs Gram-Schmidt (no-op on already-orthogonal Hadamard) — calibration_data accepted but discarded | kv_cache/squat.rs | R96 | Open |
| H60c | **kivi.rs per-channel quantization claim is FALSE.** quantize() dispatches on PerChannel/PerToken/PerGroup but computes global min/max across all dimensions regardless of QuantScheme. True per-channel quantization never implemented. PerChannel and PerToken treated identically | kv_cache/kivi.rs | R96 | Open |
| H60d | **kivi.rs SIMD dequantization explicit TODO placeholder.** dequantize_batch_avx2 contains a TODO comment and falls back to scalar loop. AVX2 intrinsics mentioned in comments but never implemented. File claims SIMD acceleration it does not provide | kv_cache/kivi.rs | R96 | Open |
| H60e | **hot_buffer.rs pop_oldest() BUG — no read cursor for sequential pops.** pop_oldest() assumes oldest_pos=0. After one pop, the next pop returns index 0 again (not the new oldest), producing stale data. No read_pos cursor maintained — repeated sequential pops are semantically broken | kv_cache/hot_buffer.rs | R96 | Open |
| H60f | **operators.rs only 3/10 attention types SQL-dispatchable.** ruvector_attention_score() collapses all non-ScaledDot/FlashV2 types to ScaledDot fallback. Linear, GAT, Sparse, MoE, Cross, Sliding, Poincare registered in ruvector_attention_types() but cannot be dispatched from SQL | attention/operators.rs | R96 | Open |
| H60g | **flash.rs is not a PostgreSQL function despite living in pgrx crate.** Zero #[pg_extern] macros in main code path. All computation on in-memory &[f32] slices, not SQL result sets or PostgreSQL data types. Unreachable from SQL | attention/flash.rs | R96 | Open |
| H60h | **flash.rs false O(sqrt(N)) space complexity claim.** Module docstring claims O(sqrt(N)) space. Actual: block_outputs Vec collects all blocks before combining — peak memory is O(N). Only KV-dimension tiling; query tiling abandoned (block_size_q dead_code) | attention/flash.rs | R96 | Open |
| H60i | **trace.rs feature gate claim is documentation-only — compiles unconditionally.** Module doc says "available only with trace feature flag" but no #[cfg(feature="trace")] attribute exists anywhere. TraceState and all diagnostics compile in every build | trace.rs | R96 | Open |
| H60j | **policy.rs age-based eviction only — NO H2O, attention-sink, StreamingLLM, or LRU.** should_evict() is solely positional age (total_tokens - position). No attention-score weighting. Semantically critical mid-sequence tokens evicted identically to noise tokens | kv_cache/policy.rs | R96 | Open |
| H60k | **policy.rs RematerializationPolicy evaluate/tracker pressure divergence.** evaluate() uses raw local pressure ratio rather than memory_tracker.pressure(). Tracker state and evaluate() can diverge if update_memory() not called before evaluate() | kv_cache/policy.rs | R96 | Open |
| H61 | **hyperbolic/adapter.rs Poincare-only despite "adapter" name — no Lorentz/hyperboloid conversion.** The file name implies interoperability between hyperbolic models (Poincare ball ↔ hyperboloid). No x₀=(1+‖x‖²)/(1-‖x‖²) or xᵢ=2xᵢ/(1-‖x‖²) maps exist. prime-radiant hyperbolic system is locked to one model | hyperbolic/adapter.rs | R97 | Open |
| H62 | **Prime-Radiant hyperbolic HNSW is O(n) brute-force stub.** adapter.rs search() and mod.rs similarity search both use linear scans over all vectors. index_built flag is written but never read — no actual HNSW index construction. Comment: "In production, this would use ShardedHyperbolicHnsw." Contrast with ruvector-hyperbolic-hnsw (genuine native HNSW, R92) | hyperbolic/adapter.rs, hyperbolic/mod.rs | R97 | Open |
| H63 | **HyperbolicEnergy::merge() silently corrupts cross-curvature aggregation.** Lines 179-187 merge two HyperbolicEnergy objects without checking that curvatures match. Energies computed under different curvatures (e.g., -1.0 and -2.0) are added directly — hyperbolic distances scale differently under different curvatures, so the merged total_energy is mathematically meaningless. No assertion or warning emitted | hyperbolic/energy.rs | R97 | Open |
| H64 | **exp_map uses Euclidean addition instead of Mobius addition — approximate geodesic stepping only.** adapter.rs exp_map (line 173) adds the tangent result directly to the base point using standard addition rather than Mobius addition ⊕_c. Standard Poincare exponential map requires x ⊕_c (tanh(√c·‖v‖/2)·v/‖v‖) to produce an exact geodesic step. The current implementation is an approximation that accumulates error across iterative Frechet mean steps | hyperbolic/adapter.rs | R97 | Open |
| H65 | **Prime-Radiant hyperbolic module disconnected from sheaf substrate.** hyperbolic/mod.rs has ZERO imports from prime-radiant's restriction.rs, coherence.rs, or knowledge_graph.rs. The hyperbolic geometry layer is an isolated add-on with no integration into the CSR sparse sheaf Laplacian or witness_log hash chains that define prime-radiant's core identity | hyperbolic/mod.rs | R97 | Open |
| H66 | **attention/mod.rs "39 mechanisms" claim is 4x inflation — only 10 enum variants defined.** AttentionType enum has 10 variants (ScaledDot, MultiHead, Flash, Linear, LocalGlobal, Hyperbolic, LorentzCascade, EdgeFeatured, MoE, Graph). Module doc claims 39. The 18+ "real implementations" count (R37) referred to impl files; the exported type system supports only 10 named types | attention/mod.rs | R97 | Open |
| H67 | **MinCut-Gated-Transformer TransformerConfig covers only ~40% of subsystem surface.** config.rs defines sequence, hidden, KV layer counts and GatePolicy, but has NO fields for: energy gate hyperparameters (alpha/beta/gamma EBT weights), spike-driven LIF/STDP neuron parameters (tau_m, tau_s, membrane threshold), quantization bit-width selection, KV cache tier boundaries (Hot/Warm/Archive capacity), or MoD routing thresholds (lambda_delta). Five out of nine DEEP-read subsystems are unconfigurable via config.rs | config.rs | R97 | Open |
| H68 | **kv_cache/tier.rs uses age-based (positional) tier assignment, not access-frequency.** TierBoundary.tier_for_position() computes age as (current_len - position - 1) and delegates to tier_for_age(). The Hot/Warm/Archive names suggest LRU or access-frequency semantics, but recent-position tokens are always "hot" regardless of how often they are accessed. Frequently accessed mid-sequence tokens still age into cold tiers | kv_cache/tier.rs | R97 | Open |
| H69 | **spike.rs is a scheduling/dispatch layer, not a spiking neuron implementation.** spike.rs contains SpikeScheduler for Q15-gated token dispatch — it reads SpikePacket (LIF output from spike_driven.rs) but has ZERO neuron dynamics: no membrane_potential, no spike threshold, no STDP weight updates. The file consumes spike signals produced elsewhere; it does not generate them | spike.rs | R97 | Open |
| H70 | **lorentz.rs (ruvector-postgres) has no manifold validation — distance() accepts off-hyperboloid points silently.** is_on_hyperboloid() is defined but never called internally. Callers passing arbitrary x₀²-‖x‖²≠1 points get numerically incorrect distances with no error or panic. Contrast with poincare.rs which clamps ‖x‖ to MAX_NORM=0.99999 | hyperbolic/lorentz.rs | R98 | Open |
| H71 | **lorentz.rs has no pgx annotations — no SQL exposure despite living in ruvector-postgres.** Zero #[pg_extern] macros. All functions are pure Rust and only accessible via operators.rs re-export. A SQL caller wanting a Lorentz-model distance function cannot reach it directly | hyperbolic/lorentz.rs | R98 | Open |
| H72 | **norm.rs is the only MinCut kernel with zero SIMD.** LayerNorm and RMSNorm are pure scalar loops while all sibling kernel files (qgemm.rs AVX2+NEON, ffn.rs Padé+Newton-Raphson) have real SIMD intrinsics. The missing optimization is conspicuous given that LayerNorm is on the critical path for every transformer forward pass | norm.rs | R98 | Open |
| H73 | **norm.rs RMSNorm feature-flag body duplication is dead code.** `#[cfg(feature = "rmsnorm")]` block and the unconditional block contain byte-identical implementations. Any change to one must be manually mirrored to the other — a maintainability trap. The feature flag provides no behavioral variation | norm.rs | R98 | Open |
| H74 | **depth.rs curvature parameter stored but never modulates calculations — effectively hardcoded -1.0.** HyperbolicDepth.curvature field exists and is passed through constructors, but sqrt(-c) in the depth formula always evaluates to sqrt(1.0)=1.0 when c=-1.0 (the only tested value). All 6 tests use curvature=-1.0; multi-curvature behavior is untested and likely incorrect | hyperbolic/depth.rs | R98 | Open |
| H75 | **depth.rs level thresholds hardcoded with no mathematical justification.** Thresholds [0.5, 1.0, 2.0, 3.0] determining Root/High/Mid/Deep/VeryDeep hierarchy levels are magic numbers. No derivation from curvature, dimensionality, or empirical calibration. Changing curvature without changing thresholds produces meaningless level assignments | hyperbolic/depth.rs | R98 | Open |
| H76 | **config.rs HNSW parameters are dead configuration — the HNSW back-end is never used.** R97 confirmed prime-radiant hyperbolic search is O(n) brute-force with "In production would use ShardedHyperbolicHnsw" comment. HyperbolicConfig.hnsw_m (16) and hnsw_ef_construction (200) are Euclidean-HNSW defaults, not Poincare-tuned, and are never read by any search or index-build code | hyperbolic/config.rs | R98 | Open |
| H77 | **tangent.rs TangentPruner.search() is O(N) linear scan by design — no HNSW graph traversal.** search() iterates over the entire vector store to build the candidate pool before pruning. The pruner is designed to receive candidates from caller-level HNSW traversal, but no caller wires HNSW output into it. Standalone use is O(N) brute-force despite the file being part of an HNSW crate | tangent.rs | R99 | Open |
| H78 | **tangent.rs dead import: norm_squared imported from poincare.rs but never called.** The symbol appears in the use statement at the top of the file and is never referenced in any function body. Minor dead-code signal | tangent.rs | R99 | Open |
| H79 | **gnn-node/lib.rs hierarchical_forward() deserializes GNN layer configs from JSON strings on every call.** Each invocation of hierarchical_forward() on the NAPI binding parses a JSON string (GnnLayerConfig) to reconstruct the layer configuration. For high-frequency inference loops this adds serde deserialization overhead that could be avoided by caching the parsed config or accepting a typed NAPI object | crates/ruvector-gnn-node/src/lib.rs | R99 | Open |
| H80 | **GNN binding layers (node + wasm) export inference-only APIs — training APIs absent from FFI surface.** Neither gnn-node/lib.rs nor gnn-wasm/lib.rs exposes train(), backprop(), update_weights(), or any gradient computation method. The ruvector-gnn core crate contains training code (confirmed R94: scheduler.rs, replay.rs) but it is unreachable from Node.js or WASM consumers | crates/ruvector-gnn-node/src/lib.rs, crates/ruvector-gnn-wasm/src/lib.rs | R99 | Open |
| H81 | **hyperbolic_attention.rs adaptive_curvature is a dead field.** The `adaptive_curvature` field is set at construction from a config flag but never updated during compute(). Curvature is effectively fixed at construction time despite the "adaptive" naming, negating the ability to dynamically modulate the hyperbolic geometry | hyperbolic_attention.rs (ruvector-attention) | R100 | Open |
| H82 | **mixed_curvature.rs linear blend of two softmax distributions is mathematically imprecise.** Blending two independent softmax-normalized score distributions with scalar weights does not produce a proper mixture model — the result is only approximately normalized and loses the geometric meaning of each manifold's attention distribution. Renormalization rescues numerical correctness but not semantic rigor | mixed_curvature.rs (ruvector-attention) | R100 | Open |
| H83 | **attention/linear.rs is a PLACEHOLDER with no O(n) kernel math — deflates MinCut linear attention novelty.** LinearAttention struct exists with config fields but has NO forward() method and NO ELU+1 feature map approximation (the key contribution of Katharopoulos 2020). The docstring cites the paper and claims O(n) complexity but the implementation is entirely empty. Hidden behind the `linear_attention` feature gate so it does not appear in default builds. Any claim that MinCut-Gated-Transformer supports linear attention is false | attention/linear.rs (ruvector-mincut-gated-transformer) | R100 | Open |
| H84 | **lib.rs (ruvector-hyperbolic-hnsw) has no bounds check on shard index in tangent pruning.** The tangent-space pruning path computes log_c(x) at a shard centroid keyed by hierarchy depth without validating that the depth value corresponds to an existing shard. An out-of-range depth would index into an invalid shard, producing a panic or incorrect centroid. No defensive bounds check or fallback is present | lib.rs (ruvector-hyperbolic-hnsw) | R100 | Open |
| H85 | **Postgres hyperbolic/ has ZERO manifold validation at any layer** — operators.rs passes points to PoincareBall.distance() without checking |x|<1. Combined with R98 H70 (lorentz.rs accepts off-hyperboloid points), the entire postgres hyperbolic SQL API has no point-on-manifold enforcement. Invalid inputs produce silently wrong distances | hyperbolic/operators.rs, hyperbolic/lorentz.rs | R101 | Open |
| H86 | **mmap_fixed.rs advertises MmapManager + MmapGradientAccumulator but both are ABSENT** — module doc describes two structs, imports memmap2/UnsafeCell/RwLock/File but only AtomicBitmap exists. "Fixed" mmap management was planned but never implemented | mmap_fixed.rs (ruvector-gnn) | R101 | Open |
| H87 | **Healing engine global OnceLock singleton limits per-connection isolation** — HEALING_ENGINE is process-wide OnceLock<Arc<RwLock<HealingEngine>>>. All PostgreSQL connections share one healing state. Per-schema or per-connection healing policies impossible | healing/mod.rs | R101 | Open |
| H88 | **OutcomeTracker cloned into RemediationEngine AND kept as self.tracker — diverging state risk** — If OutcomeTracker is not Arc-backed, the two instances diverge after first mutation. Healing outcomes may be recorded in one copy but not the other | healing/mod.rs | R101 | Open |
| H89 | **ruvector_healing_set_thresholds takes read lock for mutation** — functions.rs uses engine.read() (shared lock) then calls detector.update_thresholds(). ruvector_healing_configure correctly uses engine.write(). Potential soundness issue if update_thresholds requires &mut self | healing/functions.rs | R101 | Open |
| H90 | **ruvector_message_pass is FACADE — returns human-readable format string** — pg_extern function accepting node_table, edge_table, embedding_col, hops, layer_type returns "Multi-hop gcn message passing over N hops from table X..." with zero SQL execution | gnn/operators.rs | R101 | Open |
| H91 | **Postgres GNN is self-contained reimplementation, NOT ruvector-gnn composition** — gnn/operators.rs imports from super::gcn, super::graphsage, super::aggregators (local postgres siblings). Zero imports from the ruvector-gnn crate (11+ DEEP files from R91/R94/R99). Two independent GNN ecosystems coexist | gnn/operators.rs, gnn/gcn.rs | R101 | Open |
| H92 | **gcn.rs has no SQL integration despite living in ruvector-postgres** — Purely in-memory Vec<Vec<f32>> operations. Graph topology passed as &[(usize, usize)] edge lists. The SQL bridge presumably lives in workers/gnn.rs (not yet read) | gnn/gcn.rs | R101 | Open |
| H93 | **gcn.rs missing self-loops — pure neighbor aggregation, not canonical GCN** — update() ignores _node_features parameter. Full Kipf & Welling requires A+I (adjacency + identity) before normalization to include node's own features. Current implementation is neighbor-only aggregation | gnn/gcn.rs | R101 | Open |
| H94 | **GCN/GraphSAGE use deterministic pseudo-random weights, no trained weight loading** — GCNLayer::new() uses val = ((i * out_features + j) * 0.01) % 1.0. Comment labels it "Simple deterministic initialization for testing." No weight loading path exists — all postgres GNN inference produces structurally consistent but semantically meaningless results | gnn/operators.rs, gnn/gcn.rs | R101 | Open |
| H95 | **spectral.rs spectral_distance() is asymmetric — distance(a,b) ≠ distance(b,a)** — normalizes by mean(|b|), making drift magnitude depend on comparison order. drift_trend() iterates newest-to-oldest while detect_drift() goes oldest-to-newest, amplifying inconsistency | coherence/spectral.rs | R105 | Open |
| H96 | **spectral.rs no runtime warning when using incorrect fallback** — without `spectral` feature, silently uses broken power iteration + deflation. No log/error/warning emitted to alert callers | coherence/spectral.rs | R105 | Open |
| H97 | **incremental.rs energy_trend() slope sign is inverted** — reversed iter + forward index produces backward time-axis. Callers interpreting trend direction get the wrong sign. Fix: collect in forward order or negate | coherence/incremental.rs | R105 | Open |
| H98 | **sheaf/attention.rs no backward pass — TrainableAttention NOT implemented** — forward() exists but no gradient computation. RestrictionMap has update_weights()/update_bias() stubs. End-to-end training impossible without external autograd | sheaf/attention.rs | R105 | Open |
| H99 | **sheaf/attention.rs fixed seed=42 for all restriction maps** — RestrictionMap::from_config() uses hardcoded LCG seed. All SheafAttention::new() instances produce identical initial maps. Multi-head/multi-layer diversity requires explicit with_restriction_maps() | sheaf/attention.rs, sheaf/restriction.rs | R105 | Open |
| H100 | **sheaf/attention.rs multi-head is structurally single-head** — config.num_heads declares count but only ONE set of (rho_query, rho_key, rho_value) restriction map triples created. True multi-head sheaf attention requires num_heads independent triples | sheaf/attention.rs | R105 | Open |
| H101 | **sheaf/attention.rs compute_energy_matrix() not used in forward path** — batch method exists but forward() computes energies per-pair. Batch inference cannot leverage it without external orchestration | sheaf/attention.rs | R105 | Open |
| H102 | **sheaf/early_exit.rs no type-level binding between energy_fn and sheaf Laplacian** — energy_fn is opaque caller-supplied. Nothing prevents passing L2 norm instead of Dirichlet energy. Mismatch silently degrades quality | sheaf/early_exit.rs | R105 | Open |
| H103 | **sheaf/sparse.rs compute_sparse() ignores precomputed CSR and residuals** — uses O(n_q * nnz) linear scan per query instead of already-built CSR. Also recomputes restriction_map.energy() per connection, discarding stored residuals | sheaf/sparse.rs | R105 | Open |
| H104 | **working_memory.rs attention decay makes scratchpad entries invisible after ~46 min.** Exponential decay with rate=0.1/min halves weight in ~6.93 min; at min_threshold=0.05 the entry becomes invisible after ~46 min. High-attention old entries are then evicted by FIFO before low-attention new entries, defeating the design intent | context/working_memory.rs | R104 | Open |
| H105 | **working_memory.rs has no integration with AgenticMemory or context_manager.rs.** Attention weights on variables/tasks are computed but never used for scratchpad eviction or promotion to episodic memory. WorkingMemory is a standalone struct with no cross-module wiring | context/working_memory.rs | R104 | Open |
| H106 | **semantic_cache.rs GENUINE HNSW integration — confirms ruvllm context uses real vector similarity.** semantic_cache.rs and episodic_memory.rs both use ruvector_core::index::hnsw::HnswIndex with DistanceMetric::Cosine. This is a KEY POSITIVE: the ruvllm context module is NOT using hash-based embeddings for semantic search (unlike claude_flow/ training files) | context/semantic_cache.rs, context/episodic_memory.rs | R104 | Open |
| H107 | **episodic_memory.rs has TWO parallel episodic systems in same module.** EpisodicMemory (this file, rich: full Trajectory/Episode/MemoryCompressor) and AgenticMemory.episodic field (agentic_memory.rs, thin: hollow Trajectory with steps:vec![]). No composition between them — callers must choose one, with no guidance | context/episodic_memory.rs | R104 | Open |
| H108 | **consolidation.rs consolidation_count never incremented — stats permanently zero.** consolidate_patterns() takes &self (not &mut self) so cannot mutate consolidation_count or total_consolidated. ConsolidatorStats::consolidation_count() always returns 0. Bug: likely intended as &mut self | reasoning_bank/consolidation.rs | R104 | Open |
| H109 | **consolidation.rs consolidate_fisher() destroys per-pattern Fisher importance.** Replaces all per-pattern Fisher entries with a single averaged entry at ID 0. After this call, apply_constraint() and regularization_loss() use the global average for all patterns (ID 0 lookup fails for specific IDs), permanently losing per-dimension importance distinctions | reasoning_bank/consolidation.rs | R104 | Open |
| H110 | **claude_flow_bridge.rs store_pattern() increments stats.stores BEFORE CLI call.** On CLI failure, stores count is inflated while success counter is not incremented — stats are inconsistent on error | context/claude_flow_bridge.rs | R104 | Open |
| H111 | **claude_flow_bridge.rs bad search results cached for up to 5 minutes.** cache key ns:query:limit populated from raw CLI output. If parse_search_results() misparses malformed CLI output, bad result is cached for cache_ttl_seconds (default 5 min) with no invalidation on error | context/claude_flow_bridge.rs | R104 | Open |
| H112 | **claude_flow_bridge.rs retrieve_pattern() fabricates creation timestamp.** created_at set to Utc::now() (retrieval time), not actual creation time. tags and metadata always empty Vec/HashMap regardless of CLI output | context/claude_flow_bridge.rs | R104 | Open |
| H113 | **claude_flow_bridge.rs 5th routing surface — parallel to ADR-008/model-routing.** get_routing_suggestion() calls hooks route --task and parses "Recommended agent: coder (confidence: 0.85)" string format. No structured schema. This is a 5th routing system alongside LLMRouter (R40), RuvLLMOrchestrator (R44), hnsw_router.rs (R37), and task_classifier.rs | context/claude_flow_bridge.rs | R104 | Open |
| H114 | **flow_optimizer.rs get_results() reports fabricated latency/memory improvement percentages.** Hardcoded constants (10/25/40% latency, 20/40/60% memory) keyed on optimization_level enum. No actual benchmarking, profiling, or runtime measurement | claude_flow/flow_optimizer.rs | R104 | Open |
| H115 | **task_classifier.rs not connected to ADR-008 routing.** Outputs recommended_agents and complexity score but they are never consumed by model_router.rs, hooks_integration.rs, or hnsw_router.rs. Classifier is dead-end computation in current integration | claude_flow/task_classifier.rs | R104 | Open |
| H116 | **spectral.rs detect_drift() compares only two consecutive snapshots — noisy single-step delta.** Compares current vs previous (index len-2). Single-step delta, not window-based. Will trigger on any single outlier eigenvalue measurement. Contrast: drift_trend() computes proper window average but is used only by is_drifting() | coherence/spectral.rs | R105 | Open |
| H117 | **spectral.rs power_iteration() deterministic init may lock to trivial eigenvalue.** Initializes as [1/n, 2/n, ..., 1.0] — not random. For Laplacian matrices the all-ones vector is always an eigenvector; this deterministic init may bias convergence to that trivial eigenvalue | coherence/spectral.rs | R105 | Open |
| H118 | **spectral.rs fallback compute_eigenvalues() targets largest eigenvalue, not Fiedler value.** Sheaf Laplacian analysis requires smallest nonzero eigenvalues (algebraic connectivity = Fiedler value). Power iteration finds dominant (largest) eigenvalue only. Combined with broken deflation (C32), fallback path is mathematically inappropriate for coherence analysis | coherence/spectral.rs | R105 | Open |
| H119 | **incremental.rs energy_trend() slope sign is inverted.** Reversed iter + forward index produces backward time-axis. Callers interpreting trend direction get the wrong sign — increasing coherence appears as decreasing. Fix: collect in forward order or negate the slope | coherence/incremental.rs | R105 | Open |
| H120 | **ruvector_integration.rs response_embedding copy bug** — learn_from_outcome() sets response_embedding = embedding.to_vec() (identical to query_embedding). Should capture model output, not duplicate input. Corrupts SONA trajectory data, preventing meaningful query/answer distinction | ruvector_integration.rs | R106 | Open |
| H121 | **ruvector_integration.rs three write-only config fields** — enable_attention, enable_graph, enable_gnn set from capability flags but never read in routing, search, or initialization. Graph/GNN subsystems never conditionally activated | ruvector_integration.rs | R106 | Open |
| H122 | **ruvector_integration.rs two independent UnifiedIndex instances never synchronized** — RuvectorIntegration creates IntelligenceLayer (with private UnifiedIndex) AND separate self.unified_index. Vectors added via add_vector() invisible to route_with_intelligence() and vice versa | ruvector_integration.rs | R106 | Open |
| H123 | **coherence.rs silent embedding degradation** — all three public methods accept Option<&[Vec<f32>]>. Passing None silently activates hash embeddings (19th instance) with no warning, log, or quality flag. External callers cannot determine from result whether they got hash-based or real-embedding scores | quality/coherence.rs | R106 | Open |
| H124 | **coherence.rs 4 of 6 ContradictionType variants dead code** — Temporal, AttributeMismatch, Causal, Contextual never emitted by any detection branch. Similarly 4 of 6 ViolationType variants dead | quality/coherence.rs | R106 | Open |
| H125 | **diversity.rs 60%-weighted semantic path driven by hash embeddings** — default diversity: 40% lexical + 60% semantic. When no external embeddings provided (common case), the dominant semantic component uses hash pseudo-embeddings making scores unreliable | quality/diversity.rs | R106 | Open |
| H126 | **adapter.rs forward_sequential() is mathematically wrong** — applies each adapter to running accumulated sum then subtracts original input. Double-removes input instead of returning clean accumulated delta. Produces incorrect multi-adapter compositions | lora/adapter.rs | R106 | Open |
| H127 | **adapter.rs ensure_capacity() TOCTOU race** — snapshots ref_counts from DashMap, sorts, then removes. Concurrent thread can acquire reference between snapshot and remove(), potentially evicting a live adapter | lora/adapter.rs | R106 | Open |
| H128 | **training.rs GradientAccumulator dead in main path** — fully implemented and initialized in init_for_lora() but train_step() never calls accumulator.accumulate(). Accumulator populated by init but never used during training | lora/training.rs | R106 | Open |
| H129 | **training.rs EwcRegularizer.update_fisher() never called** — Fisher information initialized at startup but never updated during training loop. Makes EWC regularization static (from initial weights) rather than adaptive to training trajectory | lora/training.rs | R106 | Open |
| H130 | **kv_cache_manager.rs deadlock risk in swap_out()** — double-acquisition of allocations RwLock (read then write) while holding free_blocks.write(). Lock ordering not globally consistent across five RwLock fields. Concurrent calls with allocate() or extend() can deadlock | serving/kv_cache_manager.rs | R106 | Open |
| H131 | **kv_cache_manager.rs memory estimate 2x too low** — bytes_per_block() multiplies by 2 for f16 but actual storage is f32 via TwoTierKvCache. total_memory() and stats systematically report half true heap footprint | serving/kv_cache_manager.rs | R106 | Open |
| H132 | **kv_cache_manager.rs swap_out() broken block accounting** — does not call allocated_blocks.fetch_sub() when freeing blocks. Only free() performs subtraction. After any swap-out, stats().allocated_blocks overcounts and block_utilization() inflates | serving/kv_cache_manager.rs | R106 | Open |
| H133 | **LineageBuilder.build() uses InvalidOperation error with sentinel EntityRef for missing entity_ref field.** Produces misleading "Invalid operation CREATE for entity unknown:unknown". The error variant is semantically wrong — the actual problem is a missing required field, not an invalid operation on a known entity. Callers cannot distinguish this from a genuine operation error | governance/lineage.rs | R108 | Open |
| H134 | **EntityLineageTracker.add() uses timestamp ordering for causal sequence but provides no clock-skew protection.** In a distributed system with concurrent writes, two records can have identical or inverted timestamps, silently collapsing causal order. The DAG dependencies Vec<LineageId> encodes true causality — timestamp is redundant and fragile. Repository.rs correctly uses Kahn topological sort instead | governance/lineage.rs | R108 | Open |
| H135 | **policy.rs EscalationCondition DSL has NO evaluator.** Enum defines EnergyAbove(f32), PersistentEnergy, SpectralDrift, ConsecutiveRejections, All, Any — complete condition algebra — but no eval(), no match arm anywhere that tests rules against live state. Escalation rules are sorted and stored but never executed | governance/policy.rs | R108 | Open |
| H136 | **policy.rs content_hash() omits key fields.** Blake3 hash excludes: description, EscalationCondition variant/parameters, allowed_approvers, custom_thresholds, supersedes. Two bundles with different approvers or escalation payloads produce same hash if names match | governance/policy.rs | R108 | Open |
| H137 | **policy.rs supersede() silently drops successor_id parameter.** Method accepts PolicyBundleId but variable goes unused — only self.status = Superseded is set. Parameter is documentation fiction | governance/policy.rs | R108 | Open |
| H138 | **policy.rs unauthorized-approver error misclassified as DuplicateApprover.** When allowed_approvers is non-empty and unauthorized approver calls add_approval(), error returned is DuplicateApprover — semantically wrong. Retry logic for duplicates would inadvertently retry authorization failures | governance/policy.rs | R108 | Open |
| H139 | **witness.rs with_actor() post-construction mutation recomputes hash but builder head and cloned copies diverge silently.** Any copy taken before with_actor() retains stale hash. Chain integrity depends on which reference is used | governance/witness.rs | R108 | Open |
| H140 | **witness.rs verify_chain_link() returns PreviousNotFound for wrong-predecessor case.** "Not found" vs "wrong predecessor" error conflation — callers cannot distinguish missing record from chain corruption | governance/witness.rs | R108 | Open |
| H141 | **governance/mod.rs GovernanceError::Repository(String) is stringly typed.** Repository errors lose structured information at the boundary. PolicyError, WitnessError, LineageError have proper #[from] wrappers but RepositoryError does not exist as a type | governance/mod.rs | R108 | Open |
| H142 | **storage/mod.rs HybridStorage wraps FileStorage ONLY — postgres never wired.** Single field: file_storage: FileStorage. No Option<PostgresStorage>. has_postgres() returns a bool from config but result never used for backend selection. "Hybrid" name is misleading | storage/mod.rs | R108 | Open |
| H143 | **storage/mod.rs StorageFactory ignores postgres_url.** create_graph_storage and create_governance_storage branch only on graph_path.is_empty(). No branch for postgres_url.is_some(). PostgresStorage unreachable via factory regardless of config | storage/mod.rs | R108 | Open |
| H144 | **storage/memory.rs witnesses_by_action never populated — governance witness lookup silently broken.** store_witness() should write to witnesses_by_action HashMap but doesn't. get_witnesses_for_action() always returns empty Vec. Trait design gap: store_witness(&[u8]) has no action_id parameter | storage/memory.rs | R108 | Open |
| H145 | **storage/file.rs unbounded cache load on startup.** load_cache() reads ALL node/edge files into RAM with no size cap. Edge filename parsing uses splitn(2, '_') — valid IDs containing underscores misparsed | storage/file.rs | R108 | Open |
| H146 | **lora/adapters/trainer.rs dual early-stopping counters track separate best-loss variables.** Step-level best (local) and epoch-level best (field) updated independently. Checkpoints saved when step-level improves; early stopping fires when epoch-level stagnates. Can diverge | lora/adapters/trainer.rs | R108 | Open |
| H147 | **lora/adapters/trainer.rs SyntheticDataGenerator quality scores semantically meaningless.** Task-type quality heuristics operate on random vectors (e.g., "coder" quality = mean abs value of first quarter). Zero real training signal about coding ability | lora/adapters/trainer.rs | R108 | Open |
| H148 | **lora/adapters/mod.rs square matrix assumption wrong for real transformers.** to_micro_lora_config(hidden_dim) sets in_features=out_features=hidden_dim. Real LoRA needs separate dims (Q: [d_model, d_head], FFN: [d_model, 4*d_model]). All 5 presets affected | lora/adapters/mod.rs | R108 | Open |
| H149 | **lora/adapters/mod.rs parallel abstraction to parent adapter.rs.** Defines own LoraConfig type with no import from lora::adapter. Two independent adapter layers in same crate, zero cross-reference | lora/adapters/mod.rs | R108 | Open |
| H150 | **sheaf/router.rs theta_deep config field is dead code.** SheafRouterConfig includes theta_deep: f32 threshold but route_by_energy() has no branch for it — the 5-level routing schema degrades to 4 levels silently. Deployed routers can never trigger the Deep Contemplation path | sheaf/router.rs | R107 | Open |
| H151 | **sheaf/router.rs confidence=1.0 hardcoded for all RoutingDecision outputs.** Comment: "Can be refined based on energy distance to threshold." All callers receive maximum confidence regardless of actual gate energy proximity to decision boundary | sheaf/router.rs | R107 | Open |
| H152 | **sheaf/router.rs tune_thresholds() adaptive mechanism structurally decoupled — no internal feedback loop.** TokenRouter accumulates no routing outcome history internally. Caller must externally observe decisions, compute new thresholds, and call tune_thresholds() manually. SONA interface passive: update_sona_model() exists but nothing in the router invokes it on each decision | sheaf/router.rs | R107 | Open |
| H153 | **agent_router.rs 6th routing surface parallel to hnsw_router.rs, model_router.rs, ruvector_integration.rs.** AgentRouter (keyword + SONA feedback) runs independently with no composition bridge to the other routing systems | claude_flow/agent_router.rs | R107 | Open |
| H154 | **lora/adapters/merge.rs merge_task_arithmetic() delegates unconditionally to merge_weighted().** Task arithmetic (Ilharco 2022) requires independent model fine-tuning delta (theta_ft - theta_pretrain). Delegating to weighted average is semantically incorrect | lora/adapters/merge.rs | R107 | Open |
| H155 | **lora/adapters/merge.rs DARE seed=42 fixed — deterministic mask defeats DARE's statistical guarantee.** DARE (Yu 2023) random mask must be independently sampled per merge call for Monte Carlo estimator properties. Fixed seed produces identical pruning every call | lora/adapters/merge.rs | R107 | Open |
| H156 | **lora/adapters/merge.rs merges A and B matrices independently — wrong for mixed-rank adapters.** merge_average() and merge_weighted() average lora_a matrices separately and lora_b matrices separately. Mixed-rank produces dimensionally invalid results | lora/adapters/merge.rs | R107 | Open |
| H157 | **lora/adapters/merge.rs no LoRA alpha scaling before any merge strategy.** Standard LoRA effective delta is (alpha/rank)*A*B. All strategies operate on raw A/B without applying scale. Merged effective weights off by rank/alpha ratio | lora/adapters/merge.rs | R107 | Open |
| H158 | **governance/repository.rs "Async-First" claim FALSE — all trait methods are synchronous.** Module doc promises async for I/O-bound persistence but all trait definitions use fn not async fn. InMemoryWitnessRepository uses blocking HashMap throughout | governance/repository.rs | R107 | Open |
| H159 | **governance/repository.rs is traits + InMemory stubs only — 12th persistence stub.** No PostgreSQL, SQLite, or ruvector-backed implementations exist despite four trait definitions (WitnessRepository, LineageRepository, PolicyRepository, GovernanceRepository) | governance/repository.rs | R107 | Open |
| H160 | **storage/postgres.rs find_similar() is O(n) brute-force — no pgvector or ANN index.** Fetches ALL nodes with matching dimension, computes cosine similarity in Rust, then sorts and returns top-k. No pgvector <-> operator or postgres-side ANN | storage/postgres.rs | R107 | Open |
| H161 | **storage/postgres.rs store_witness() race condition under concurrent inserts.** SELECT COALESCE(MAX(sequence),0)+1 without transaction or advisory lock. Two concurrent writers can compute the same sequence number | storage/postgres.rs | R107 | Open |
| H162 | **storage/postgres.rs AsyncGraphStorageAdapter does NOT implement GraphStorage or GovernanceStorage traits.** Wraps PostgresStorage with tokio::spawn_blocking but exposes only ad-hoc methods. Cannot be used polymorphically via trait-based HybridStorage | storage/postgres.rs | R107 | Open |
| H163 | **serving/request.rs stop_sequences silently ignored.** should_stop() accepts _decoded_text (leading underscore = intentional suppression) but never inspects it, delegating only to max_new_tokens and EOS token checks | serving/request.rs | R107 | Open |
| H164 | **serving/mod.rs integration tests directly compose production components without mocking.** Four tests compose ServingEngine + ContinuousBatchScheduler + KvCacheManager + RequestQueue. Failures may indicate environment issues rather than unit-level regressions | serving/mod.rs | R107 | Open |
| H165 | **quality/mod.rs exposes hash-embedding coherence and diversity without documentation warning.** Public re-export surface offers no indicator that semantic_coherence and diversity default to hash pseudo-embeddings. Consumers composing QualityWeights with semantic_coherence > 0 silently receive hash-based scores | quality/mod.rs | R107 | Open |

### 3c. MEDIUM Findings

| ID | Description | File(s) | Session | Status |
|----|-------------|---------|---------|--------|
| M1 | **tangent.rs test coverage minimal — only 2 tests for full two-phase pruning pipeline.** Tests verify construction and a single round-trip through the tangent-space filter + Poincare re-rank, but do not cover edge cases: empty candidate pools, single-vector stores, curvature changes mid-run, or TangentCache invalidation after insertions | tangent.rs | R99 | Open |
| M2 | **gnn-wasm/lib.rs browser-targeted tests are construction-only — no round-trip coverage.** Tests in the #[cfg(test)] block verify that WasmGnnGraph::new() constructs without panic and that WasmCompressor::new() initializes, but no test exercises forward(), compress()+decompress() round-trip, or differentiable_search() output correctness. Browser-targeted wasm_bindgen tests cannot run under standard cargo test | crates/ruvector-gnn-wasm/src/lib.rs | R99 | Open |
| M3 | **gnn-node/lib.rs WasmGnnGraph and WasmCompressor compression tiers use 5-tier enum with no validation of tier ordinal.** CompressionTier::from_u8() uses a match on hardcoded u8 values but does not reject unknown tier values — out-of-range input defaults silently. NAPI callers passing arbitrary integers receive the default None tier rather than an error | crates/ruvector-gnn-node/src/lib.rs | R99 | Open |
| M4 | **poincare.rs (ruvector-attention) frechet_mean uses a fixed learning rate of 0.1 with no adaptive scheduling.** At high curvature values the Riemannian gradient magnitudes grow, and a fixed 0.1 step can overshoot the Poincare ball boundary, requiring repeated projection. No damping, decay, or line search is applied. This may cause oscillation in the Fréchet mean computation for embeddings near the ball boundary | poincare.rs (ruvector-attention hyperbolic) | R100 | Open |
| M5 | **mixed_curvature.rs [euclidean \|\| hyperbolic] packing is an implicit unenforced API contract.** The module assumes that input embeddings are packed with the first half being Euclidean coordinates and the second half being Poincare-ball coordinates. This split is not validated, not type-enforced, and not documented in the function signatures. Callers passing unsplit embeddings silently receive nonsensical mixed-geometry attention scores | mixed_curvature.rs (ruvector-attention) | R100 | Open |
| M6 | **EntityLineageTracker.add() performs no duplicate-LineageId check.** Caller can insert two records with the same LineageId without error. LineageError::AlreadyExists exists but is never raised in this file. Downstream consumers of tracker.lineage Vec may encounter duplicate IDs during dependency traversal | governance/lineage.rs | R108 | Open |
| M7 | **Operation::is_mutation() excludes Delete — inconsistent operation taxonomy.** is_destructive() includes Merge AND is_mutation() also includes Merge; Delete is classified as purely destructive (not a mutation). Callers using is_mutation() to gate audit writes will miss Delete operations | governance/lineage.rs | R108 | Open |
| M8 | **LineageRecord stores authorizing_witness: WitnessId without validating the ID exists at record-creation time.** LineageError::MissingWitness is only raised by LineageBuilder when the field is absent — not when the ID refers to a nonexistent witness. Orphaned WitnessIds can be embedded in records; validation is deferred entirely to repository.rs cross-check | governance/lineage.rs | R108 | Open |
| M9 | **domain-expansion planning.rs goal matching exact string only** — no partial satisfaction or relaxation. Plans that achieve goals through indirect effects scored as failures | planning.rs | R115 | Open |
| M10 | **domain-expansion rust_synthesis.rs reference_solution() Transform-only** — DataStructure and Algorithm return None. 60-70% of task types have no baseline comparison during population evolution | rust_synthesis.rs | R115 | Open |
| M11 | **domain-expansion transfer.rs Beta distribution normal approximation** — Abramowitz and Stegun for alpha,beta > 1 with power fallback. Adequate but inexact; true Beta inverse CDF requires incomplete beta function | transfer.rs | R115 | Open |
| M12 | **postgres domain_expansion operators.rs thin entry point** — pg_extern initiates transfer and returns status JSON. No population evolution, no cost curve tracking. PostgreSQL integration is entry point not training loop | operators.rs | R115 | Open |
| M13 | **setup.ts hardcoded test credentials in docker-compose template** — POSTGRES_PASSWORD=claude-flow-test embedded in generated docker-compose.yml. Users who deploy to non-local environments inherit insecure default credentials | v3/@claude-flow/cli/src/commands/ruvector/setup.ts | R138 | Open |
| M14 | **setup.ts repeats "150x-12,500x" unsubstantiated performance claim** — help text includes the same marketing claim traced to sona-agentdb-integration.ts (R137). No benchmark code backs this number anywhere in the codebase | v3/@claude-flow/cli/src/commands/ruvector/setup.ts | R138 | Open |
| H166 | **cohomology/neural.rs SheafConvolution never applies restriction maps — reduces to standard GCN.** forward() uses identity aggregation only (comment: "For identity restriction, just add neighbor"). The general restriction map rho is never invoked. The theoretical novelty of sheaf structure is bypassed in the core operation | cohomology/neural.rs | R109 | Open |
| H167 | **cohomology/neural.rs CohomologyPooling.spectral_weighting declared and settable but pool() never uses it.** with_spectral_weighting() exists but the field is dead config. The spectral weighting branch is entirely unimplemented | cohomology/neural.rs | R109 | Open |
| H168 | **cohomology/neural.rs GELU uses sigmoid approximation with higher error than tanh-based alternative.** Uses x * sigmoid(1.702x) — the constant 1.702 is the fast-sigmoid approximation with higher error than the tanh-based approximation (0.044715 cubic). Impacts training accuracy for sheaf neural layers | cohomology/neural.rs | R109 | Open |
| H169 | **cohomology/simplex.rs Bron-Kerbosch uses naive pivot (first element of P∪X), not Tomita degeneracy variant.** Worst-case O(3^(n/3)) and in practice slow for dense graphs >50 nodes. No timeout or size guard | cohomology/simplex.rs | R109 | Open |
| H170 | **cohomology/cohomology_group.rs normalize() Gram-Schmidt bug: scale(1.0) at line 134 is a no-op.** Intent appears to be negating the projection but scale(1.0) does nothing. Manual subtraction below produces the structurally correct result but the dead call is misleading | cohomology/cohomology_group.rs | R109 | Open |
| H171 | **cohomology/cohomology_group.rs quotient_space() R-coefficient only — no Smith Normal Form for integer cohomology.** Gram-Schmidt quotient is correct over R but wrong for integer or mod-p cohomology. The config struct has no coefficient field and the R-only assumption is undocumented | cohomology/cohomology_group.rs | R109 | Open |
| H172 | **cohomology/laplacian.rs build_matrix() and apply() use identity restrictions — non-trivial sheaves silently reduced to graph Laplacian.** Both matrix assembly and matrix-free product use residual = s_val - t_val without applying restriction maps rho_s, rho_t. For general sheaves this is mathematically equivalent to a plain weighted graph Laplacian | cohomology/laplacian.rs | R109 | Open |
| H173 | **cohomology/laplacian.rs compute_spectrum() constructs O(total_dim^2) dense matrix then O(n^2 * max_iter) per eigenvalue.** No sparse representation for large sheaves. No bounds warning. For sheaves with many nodes × large stalk dimension, this is prohibitively expensive | cohomology/laplacian.rs | R109 | Open |
| H174 | **cohomology/laplacian.rs harmonic_representatives() extracts wrong eigenvectors.** Extracts zero-eigenvalue eigenvectors from the deflated largest-first power iteration — because uniform init means first eigenvector IS the constant (zero) vector, deflation removes it and subsequent iterations find the next largest (not next smallest). harmonic_representatives may return arbitrary non-harmonic vectors | cohomology/laplacian.rs | R109 | Open |
| H175 | **cohomology/obstruction.rs betti_numbers always single-element vec — only b_0-equivalent computed.** report.betti_numbers = vec![spectrum.null_space_dim]. True Betti numbers b_0, b_1, b_2, ... require separate computations per cohomology degree. Callers indexing betti_numbers[1] or higher would panic | cohomology/obstruction.rs | R109 | Open |
| H176 | **training/loss.rs SpectralRegularization.compute_with_gradients returns zero gradient vector.** Commented "simplified/auxiliary" but if composed into training loop as a Loss implementor, it silently contributes zero gradient while still adding to loss value. No panic or warning | training/loss.rs | R109 | Open |
| H177 | **training/curriculum.rs exponential decay NaN on final_temp=0.** Calls (final_temp/initial_temp).ln(). If final_temp==0.0 computes ln(0)=-inf, producing NaN output. No guard against this user-accessible edge case | training/curriculum.rs | R109 | Open |
| H178 | **training/mining.rs fixed seed=42 makes "random" mining deterministic.** random_selection() and distance_weighted_selection() both use hardcoded seed=42. Defeats the purpose of stochastic sampling in training | training/mining.rs | R109 | Open |
| H179 | **training/mining.rs InBatchMiner and HardNegativeMiner are disjoint with no composition bridge.** InBatchMiner returns index sets from a batch with no similarity computation; HardNegativeMiner requires pre-materialized embedding slices. A caller cannot use both without manual bridging code | training/mining.rs | R109 | Open |
| H180 | **cohomology/cocycle.rs is_coboundary() degree-0 path wrong on disconnected complexes.** Checks constancy (a 0-cochain is a coboundary iff it is constant). On a disconnected complex, a locally-constant function (one value per connected component) is not a coboundary in H^0 but passes the constancy check if all components share the same value | cohomology/cocycle.rs | R110 | Open |
| H181 | **cohomology/cocycle.rs Cochain add() near-cancellation truncation breaks linearity.** set() drops values below 1e-10 for sparsity; add() calls set() after summing. Near-cancellation between two cocycles silently removes simplex entries: (c1 + c2) loses entries where values nearly cancel, breaking (c1+c2) = c1+c2 algebraically | cohomology/cocycle.rs | R110 | Open |
| H182 | **cohomology/cocycle.rs SheafCoboundary.apply() assigns edge results by loop index, not stable simplex ID.** Results assigned via SimplexId::new(i as u64) using loop index i rather than a stable edge identifier from the complex. If edges are reordered, cocycle values are assigned to wrong simplices, silently corrupting all sheaf cohomology computation | cohomology/cocycle.rs | R110 | Open |
| H183 | **cohomology/cocycle.rs Hodge Laplacian via operator composition is wrong due to apply_adjoint() bug.** Coboundary.laplacian() computes L = delta^* delta + delta delta^* correctly by formula, but apply_adjoint() has the dimension indexing bug (C44). The composed Laplacian is therefore wrong — 2nd parallel broken Laplacian implementation alongside laplacian.rs | cohomology/cocycle.rs | R110 | Open |
| H184 | **cohomology/sheaf.rs RestrictionFn has no functor composition law enforcement.** Arc<dyn Fn> closures for rho_{u->v} and rho_{v->w} do not enforce rho_{u->w} == rho_{v->w} ∘ rho_{u->v}. The sheaf transitivity axiom is silently violatable for any restriction chain | cohomology/sheaf.rs | R110 | Open |
| H185 | **cohomology/sheaf.rs from_graph() bidirectional restriction maps are mathematically unrelated.** Creates maps (source->target and target->source) from DISTINCT rho_source and rho_target fields. The reverse restriction should be the adjoint/transpose of the forward. Current design: two independent functions with no mathematical relationship — not a proper sheaf on an undirected graph | cohomology/sheaf.rs | R110 | Open |
| H186 | **cohomology/sheaf.rs SheafSection.is_global flag is dead state — never set to true.** set() sets is_global=false, from_local() creates is_global=false. No method validates and sets is_global=true. The flag cannot represent a genuinely global section | cohomology/sheaf.rs | R110 | Open |
| H187 | **cohomology/diffusion.rs explicit Euler no CFL stability check.** diffuse() uses fixed dt (default 0.1) with no check against spectral radius of L. Explicit Euler for dx/dt=-Lx is stable only when dt < 2/lambda_max. For dense graphs with high edge weights, lambda_max >> 20 and dt=0.1 is numerically unstable — divergence not detected | cohomology/diffusion.rs | R110 | Open |
| H188 | **cohomology/diffusion.rs residual_obstruction is heuristic threshold, not cohomological projection.** Defined as Some(final_energy) when converged && final_energy > 0.001. A true residual obstruction is the projection of the initial section onto harmonic space H^0(X;F) = ker(L). The threshold test provides no cohomological information about what prevents diffusion convergence | cohomology/diffusion.rs | R110 | Open |
| H189 | **cohomology/diffusion.rs compute_node_energies() correct only for identity restriction maps.** Computes per-node energy as sum_e w_e ||x_v - x_u||^2. For general sheaves, edge energy should be w_e ||rho_s(x_s) - rho_t(x_t)||^2. Identity-restriction assumption undocumented and inconsistent with general sheaf claims | cohomology/diffusion.rs | R110 | Open |
| H190 | **transport/sliced_wasserstein.rs correct distributional OT (compute_distributional_ot) is dead code.** The mathematically correct implementation (comparing query to empirical CDF of sorted keys) exists but is marked #[allow(dead_code)] and never called. The live code (compute_1d_ot_distance) uses the simpler but incorrect pointwise approach | transport/sliced_wasserstein.rs | R110 | Open |
| H191 | **transport/sliced_wasserstein.rs two-stage prefilter has metric mismatch.** Dot-product prefilter selects top-C candidates then applies OT only on candidates. Conflates metric spaces: dot-product ranks by cosine similarity, OT ranks by distributional distance. Candidate set may systematically exclude keys that are OT-close but direction-far | transport/sliced_wasserstein.rs | R110 | Open |
| H192 | **transport/sliced_wasserstein.rs histogram/CDF infrastructure from cached_projections.rs never consumed.** Module docstring claims "Window-level caching of sorted projections" and "Histogram CDF for ultra-fast comparisons" but histogram path is never triggered in live compute paths. use_histograms flag controls cache construction but histogram data never read | transport/sliced_wasserstein.rs | R110 | Open |
| H193 | **transport/centroid_ot.rs Sinkhorn (namesake algorithm) is dead code with logic bug.** sinkhorn_distance() marked #[allow(dead_code)] — the full Sinkhorn log-domain transport implementation is never called. compute_with_cache uses simple softmax over centroid distances instead. Additionally, the dead sinkhorn loop has a logic bug: log_u is initialized but never updated in the iteration body | transport/centroid_ot.rs | R110 | Open |
| H194 | **sparql/triple_store.rs in ruvector-postgres crate uses ZERO Postgres.** TripleStore is 100% in-memory DashMap. No sqlx, no tokio_postgres, no pgrx. File path and crate name strongly imply Postgres-backed storage. Consistent with sparql/ast.rs (R111) and sparql_executor.rs (R34) — entire sparql/ module operates without the Postgres connection it lives alongside | sparql/triple_store.rs | R113 | Open |
| H195 | **TripleIndex enum defines 6 variants but TripleStore struct implements only 3.** SOP, PSO, OPS declared but no corresponding DashMap fields or query branches. Mixed-bound queries (Subject+Object) fall back to O(|predicates|) linear filter over SPO index instead of O(1) SOP lookup. The enum design intention is complete; the implementation is half-finished | sparql/triple_store.rs | R113 | Open |
| H196 | **rvf-node/lib.rs index_stats().layers hardcoded to 0** — "populated when active" comment with no code path that ever sets a non-zero value. Consumers of index statistics get misleading layer count | rvf-node/src/lib.rs | R121 | Open |
| H197 | **rvf-crypto/witness.rs is_multiple_of() uses nightly-only unstable API** — fails on stable Rust toolchain. Witness chain verification broken for stable channel users | rvf-crypto/src/witness.rs | R123 | Open |
| H198 | **attestation.rs verify_key_binding() structural-only** — checks platform enum + measurement bytes equality. No cryptographic verification of sealed_key_material, no unsealing, no signature check. Same unverified-crypto pattern as policy.rs (C36) | rvf-crypto/src/attestation.rs | R124 | Open |
| H199 | **attestation.rs zero TEE SDK calls** — no sgx_quote, sev_snp_report, tdx_quote. All TEE interaction deferred to nonexistent QuoteVerifier implementations | rvf-crypto/src/attestation.rs | R124 | Open |
| H200 | **level0.rs signature stored but never verified** — sig_algo, sig_length, signature_buf deserialized but read_level0() never validates. Valid magic+CRC passes regardless of signature content | rvf-manifest/src/level0.rs | R124 | Open |
| H201 | **rvf-kernel silent fallback to halt stub** — build() falls back from Docker to 4096-byte CLI+HLT bzImage stub without caller awareness. Non-bootable kernel embedded as if valid | rvf-kernel/src/lib.rs | R124 | Open |
| H202 | **rvf-kernel zero UUID/timestamp** — build_kernel_header() leaves build_id=[0u8;16], build_timestamp=0. No caller enforces. Defeats audit/reproducibility | rvf-kernel/src/lib.rs | R124 | Open |
| H203 | **rvf-kernel unsigned kernel source download** — Docker build fetches from cdn.kernel.org with no PGP/SHA verification. MITM risk on source tarball | rvf-kernel/src/lib.rs | R124 | Open |
| H204 | **rvf-launch libc_kill() misleading unsafe** — marked unsafe but contains no unsafe Rust. Spawns `Command::new("kill")` shell subprocess for SIGTERM | rvf-launch/src/lib.rs | R124 | Open |
| H205 | **rvf-launch zero host-side isolation** — no seccomp BPF, no Linux namespaces, no capability dropping before QEMU spawn. QEMU escape uncontained | rvf-launch/src/lib.rs | R124 | Open |
| H206 | **database.ts NodeBackend auto-selected but broken** — resolveBackend('auto') picks NodeBackend first. ensureOpen() only checks this.closed, no backend health validation. Broken NAPI silently propagates | npm/packages/rvf/src/database.ts | R124 | Open |
| H207 | **errors.ts fromNative() regex-based error parsing** — parses hex codes from Error.message via `/0x([0-9a-fA-F]{4})/`. NAPI errors without embedded hex codes fall back to generic BackendInitFailed | npm/packages/rvf/src/errors.ts | R124 | Open |
| H208 | **backend.ts NodeBackend.saveMappings() silently swallows errors** — bare catch{} on id→label persistence. Crash recovery produces label collision | npm/packages/rvf/src/backend.ts | R124 | Open |
| H209 | **backend.ts WasmBackend raw C-ABI memory layout fragile** — assumes exactly 8-byte id + 4-byte dist per result. No bounds checking. ABI change silently corrupts | npm/packages/rvf/src/backend.ts | R124 | Open |
| H210 | **backend.ts resolveBackend no NAPI-to-WASM fallback** — auto mode uses process.versions.node presence, not capability detection. NodeBackend load failure propagates instead of falling back | npm/packages/rvf/src/backend.ts | R124 | Open |
| H211 | **backend.ts NodeBackend.derive() nextLabel divergence** — parent and child share same nextLabel start. Post-derive parent ingests cause label collisions between parent/child stores | npm/packages/rvf/src/backend.ts | R124 | Open |
| H212 | **types.ts dimensions/dimension naming divergence** — TS `dimensions` (plural) vs Rust `dimension` (singular). mapOptionsToNative() translates but permanent schema split | npm/packages/rvf/src/types.ts | R124 | Open |
| H213 | **types.ts CompressionProfile silently ignored** — defined in TS types but absent from rvf-node/index.d.ts. Rust NAPI ignores compression setting | npm/packages/rvf/src/types.ts | R124 | Open |
| H214 | **types.ts Uint8Array vs Buffer mismatch** — RvfKernelData/EbpfData typed Uint8Array but NAPI returns Buffer. Compatible at runtime but strict TS rejects | npm/packages/rvf/src/types.ts | R124 | Open |
| H215 | **types.ts RvfIndexStats/RvfWitnessResult dead API** — defined and re-exported but no backend method returns them. Unreachable at runtime | npm/packages/rvf/src/types.ts | R124 | Open |
| H216 | **index.ts hard rvf-solver import** — static import at line 56. Missing optional peer dep crashes entire @ruvector/rvf import | npm/packages/rvf/src/index.ts | R124 | Open |
| H217 | **index.ts solver type re-exports** — 11 solver types create compile-time dependency. Bundlers may include full rvf-solver when only core features used | npm/packages/rvf/src/index.ts | R124 | Open |
| H218 | **domain-expansion rust_synthesis.rs embeddings are feature-counting heuristics** — 64-dim vector of pattern occurrence counts (.map(, for, unsafe), not neural. Same hash-based pattern, though more structured with 8 feature groups. Cross-domain cosine similarity semantically shallow | rust_synthesis.rs | R115 | Open |
| H219 | **domain-expansion rust_synthesis.rs evaluation heuristic-only** — no compilation, no execution, no test running. Scores by checking function signature presence, banned pattern absence, nested-loop heuristics. Correctness scoring maxes at ~0.8 for perfect solutions | rust_synthesis.rs | R115 | Open |

| C98 | **Solver router 4/7 algorithm backends are Jacobi with label swaps** — ForwardPush, HybridRandomWalk, BMSSP, TRUE all dispatch to solve_jacobi_fallback() then overwrite result.algorithm. Algorithm::TRUE is relabeled Neumann. No push-based, random-walk, or block multi-shift code exists | router.rs | R116 | Open |
| C99 | **contribute-daemon.js credit system trivially gameable** — contribution_credit sent with no cryptographic proof-of-work. doRealWork() burns CPU (djb2 loop) but relay cannot verify. Self-reported cpuUsage/contributionSeconds accepted on trust | contribute-daemon.js | R116 | Open |
| C100 | **join.js compute contributions are Math.random() synthetic** — joinNetworkPersistent() generates random 100-600 computeUnits on 5s interval, credits identity, writes to QDAG. Zero real vector ops or inference. localSimulation=true disables all networking | join.js | R116 | Open |
| C101 | **join.js verifySignature() accepts any 64-char string** — Byzantine detection non-functional. No actual ed25519 verification despite PiKey being available | join.js | R116 | Open |
| C102 | **attention-unified-wasm 5/22 classes call wrong Rust constructor** — WasmCriticalPath, WasmHierarchicalLorentz, WasmTemporalBTSP secretly create CausalCone objects. WasmTopological creates MinCutGated. WasmSearchConfig aliases ParallelBranchAttention+MambaConfig fields | ruvector_attention_unified_wasm.js | R116 | Open |
| C103 | **true_solver.rs self-learning/attention/WASM claims entirely false** — zero puzzle generation, zero feedback loop, zero attention mechanism, zero wasm_bindgen, uses std::time::Instant (WASM-incompatible). Claims are about a different system | true_solver.rs | R116 | Open |
| H220 | **router.rs Algorithm::TRUE is relabeled Neumann** — entire dispatch just runs NeumannSolver::new() then overwrites result.algorithm to say TRUE. No block multi-solver implementation | router.rs | R116 | Open |
| H221 | **router.rs no integration with true_solver.rs or simd.rs** — routing infrastructure exists for dispatch but imports neither module. The solver crate's three components are disconnected | router.rs | R116 | Open |
| H222 | **simd.rs AVX2-only — no AVX-512, NEON, or WASM SIMD** — one tier vs ruvector-core's 4-tier model. Scalar gather in AVX2 hot loop negates much SIMD benefit for sparse inputs | simd.rs | R116 | Open |
| H223 | **rvlite.js WASM Cypher/SQL/SPARQL "not yet implemented"** — JSDoc explicitly marks all three query methods as stubs at WASM boundary. R38's working Cypher executor is Rust-only | rvlite.js | R116 | Open |
| H224 | **rvlite.js LoraConfig.rank aliases EmbeddingConfig.dimensions** — wasm-bindgen field aliasing bug. TrmConfig.embedding_dim and hidden_dim also reuse wrong WASM exports | rvlite.js | R116 | Open |
| H225 | **real-agents.js silent fallback to hardcoded templates** — generateSmartResponse() produces 246 lines of template text when all LLM backends fail. Callers see result.fallback=true but rarely check | real-agents.js | R116 | Open |
| H226 | **real-agents.js CRDT ledger no server-side validation** — any peer can broadcast fabricated earned values via WebSocket relay to inflate balance | real-agents.js | R116 | Open |
| H227 | **real-agents.js phantom OpenAI models** — config references gpt-5.2/gpt-5.2-turbo which do not exist. All cloud calls 404 | real-agents.js | R116 | Open |
| H228 | **monitor.js p50/p99 reads bucket counts not percentiles** — Histogram.toJSON() exposes per-bucket entry counts. getMetrics() reads buckets[50]/buckets[1000] as p50/p99. All percentile values semantically wrong | monitor.js | R116 | Open |
| H229 | **contribute-daemon.js WASM identity-only** — PiKey used once for Ed25519 key generation. Never used for contribution signing, task execution, or plugin dispatch | contribute-daemon.js | R116 | Open |
| H230 | **join.js p2p.js WebRTC/DHT orphaned** — p2p.js with WebRTC, DHT, Firebase signaling exists but join.js never imports it. The real P2P stack is disconnected | join.js | R116 | Open |
| H231 | **nervous-system-wasm WTALayer.size calls btsplayer_size** — copy-paste bug from BTSPLayer. Returns wrong size for WTALayer instances | nervous_system_wasm.js | R116 | Open |
| H232 | **rvf-solver-wasm max_day bug** — PuzzleGenerator generate() returns 28 for both match arms. 31-day months capped at day 28. Dates 29-31 never generated | engine.rs | R116 | Open |
| H233 | **RuVectorIntelligence.routeTask() is GENUINE 2-plane composition** — HNSW→SONA MicroLoRA→attention scoring with 0.3/0.7 weighted fusion. First confirmed cross-subsystem pipeline. CORRECTS prior "HNSW stores never compose" finding | RuVectorIntelligence.ts | R119 | Open |
| H234 | **"Graph" in pipeline = attention TYPE not traversal** — GraphRoPeAttention is rotary position encoding for graphs, NOT graph DB lookup. Zero ruvector-graph imports in RuVectorIntelligence.ts | RuVectorIntelligence.ts | R119 | Open |
| H235 | **hybrid_search.rs 100% commented out** — Entire file is block-comment TODO. "pending graph API exposure." Zero runtime composition | hybrid_search.rs | R119 | Open |
| H236 | **semantic_search.rs graph traversal PLACEHOLDER** — find_semantic_paths() explicitly comments "placeholder for actual graph traversal logic" and returns edges=[] | semantic_search.rs | R119 | Open |
| H237 | **ruvector_integration.rs enable_graph/enable_attention flags DEAD** — Config flags set but zero graph search calls in any method body. Aspirational only | ruvector_integration.rs | R119 | Open |
| H238 | **TargetModule::Embed shape mismatch BUG** — init_for_lora() applies single in_features/out_features to ALL modules. Embedding layers need (vocab_size × embed_dim), not (hidden_dim × hidden_dim). Using Embed crashes at runtime | training.rs, micro_lora.rs | R119 | Open |
| H239 | **micro_lora.rs Embed adapter has no embedding-table awareness** — same rank-1/2 LoRA for all modules, no vocab_size, no tied-embedding handling. Embed variant is declared-but-structurally-unusable stub | micro_lora.rs | R119 | Open |
| H240 | **markov.rs Laplace smoothing u32 truncation** — apply_smoothing casts f64 smoothing as u32, truncating alpha=0.1 to 0. Lidstone smoothing silently becomes no-op | markov.rs | R119 | Open |
| H241 | **model_router.rs adaptive learning dead** — accuracy_history is in-memory Vec with 1000-entry ring buffer, no persistence, calibration_bias() has no weight update path. Learning claim is facade | model_router.rs | R119 | Open |
| H242 | **hive-mind.js queen is static text prompt** — generateHiveMindPrompt() builds fixed string piped to Claude subprocess. No runtime query capability, no skill discovery, no dynamic negotiation | hive-mind.js | R119 | Open |
| H243 | **EmbeddingService.ts semanticSearch() is O(n) brute-force** — cosine loop over in-memory corpus array. No HNSW index despite ruvector availability | EmbeddingService.ts | R119 | Open |
| C104 | **Hash embedding fallback confirmed at CLI layer** — Intelligence.embed() uses engine only if ALREADY initialized. Most hook invocations pass skipEngine:true yielding 64-dim hash. Extends C1 to CLI integration layer | npm/packages/ruvector/bin/cli.js:2784 | R135 | Open |
| C105 | **Hash embedding in MCP server — 9th instance** — IntelligenceEngine unavailable = common default. Same hash wiring failure as C1/C78/C104 | npm/packages/ruvector/bin/mcp-server.js:230 | R135 | Open |
| C106 | **SQL/Cypher/SPARQL query destruction via sanitization** — sanitizeShellArg() strips (), ;, ', ", $, {} from query strings before rvlite pass-through. Destroys most legitimate queries. Also NOT a real injection fix (strips shell chars, not SQL injection chars) | npm/packages/ruvector/bin/mcp-server.js:2888 | R135 | Open |
| C107 | **ruvllm CLI all-facades without native binary** — query/generate/route/embed return hardcoded or hash values without .node binary. Entire CLI non-functional in JS-only mode | npm/packages/ruvllm/bin/cli.js:121 | R135 | Open |
| C108 | **ruvllm training SIMULATED** — "Training loss (simulated)" printed with no actual gradient descent or model update. No backward pass, no weight modification | npm/packages/ruvllm/bin/cli.js:624 | R135 | Open |
| H244 | **ruvector CLI 4 facade commands** — graph, router, server, cluster all print "Coming Soon" with no implementation | npm/packages/ruvector/bin/cli.js | R135 | Open |
| H245 | **Export vectors EMPTY** — cli.js:1858 exports empty array regardless of store contents | npm/packages/ruvector/bin/cli.js:1858 | R135 | Open |
| H246 | **MCP 55-tool monolith** — 3,060-line single switch statement. Largest single MCP server in ecosystem | npm/packages/ruvector/bin/mcp-server.js | R135 | Open |
| H247 | **14 MCP tools delegate via execSync("npx ruvector hooks ...")** — 2-5s cold-start per call from npx resolution | npm/packages/ruvector/bin/mcp-server.js | R135 | Open |
| H248 | **11 MCP tools delegate to "npx agentic-flow@alpha"** — cross-package exec dependency on @alpha tag | npm/packages/ruvector/bin/mcp-server.js | R135 | Open |
| H249 | **rvlite O(n) brute-force search** — no HNSW index, linear scan over all vectors | npm/packages/rvlite/bin/cli.js | R135 | Open |
| H250 | **rvlite ZERO integration with main ruvector CLI** — completely independent, zero shared code or imports | npm/packages/rvlite/bin/cli.js | R135 | Open |
| H251 | **rvlite advertises SQL/Cypher/SPARQL but provides none** — query languages deferred to SDK, CLI has no implementation | npm/packages/rvlite/bin/cli.js | R135 | Open |
| H252 | **ruvllm embedding benchmark uses hash fallback** — all benchmark results meaningless without native binary | npm/packages/ruvllm/bin/cli.js | R135 | Open |
| H253 | **ruvllm zero ruvector RAG integration** — no HNSW, no vector store, no @ruvector imports | npm/packages/ruvllm/bin/cli.js | R135 | Open |
| H254 | **setup.ts does NOT configure backend factory** — scaffolding only. Generates docker-compose + SQL schema but zero interaction with factory.ts 5-tier fallback. Even after successful setup, claude-flow memory continues using in-process sql.js | v3/@claude-flow/cli/src/commands/ruvector/setup.ts | R138 | Open |
| H255 | **setup.ts SQL calls extension C functions that fail on version mismatch** — init-db.sql invokes ruvector_exp_map, ruvector_poincare_distance, etc. Control file declares extension version 2.0.0 but SQL schema references 0.1.0. Version conflict causes function resolution failure at CREATE EXTENSION time | v3/@claude-flow/cli/src/commands/ruvector/setup.ts | R138 | Open |
| C109 | **"Integration" test files are 100% mock** — e2e_integration_test.rs (1535 LOC) and ruvllm_integration_tests.rs (1393 LOC) combined 2,928 LOC of "integration tests" contain zero cross-crate imports. Both use inline reimplementations with APIs that don't match production. No actual integration path is tested | crates/ruvllm/tests/e2e_integration_test.rs, crates/prime-radiant/tests/ruvllm_integration_tests.rs | R139 | Open |
| C110 | **Distributed Raft test nodes are facades** — 5-node docker-compose cluster runs shell scripts with netcat responding "200 OK". Nodes never invoke any Rust binary. .rlib static libraries copied to runtime image cannot be executed. Distributed deployment architecture has NEVER been validated multi-process | tests/integration/distributed/docker-compose.yml | R139 | Open |
| H257 | **ruvllm_integration_tests.rs cfg(feature="ruvllm") is misleading** — feature gate controls file compilation but the file has ZERO imports from either prime_radiant:: or ruvllm::. The gate creates false appearance of cross-crate testing | crates/prime-radiant/tests/ruvllm_integration_tests.rs | R139 | Open |
| H258 | **Mock APIs don't match production APIs** — SheafCoherenceValidator mock uses pairwise cosine similarity; real uses CoherenceEngine+SheafGraph+PolicyBundleRef. PatternToRestrictionBridge mock uses Vec<LearnedPattern>; real uses ReasoningBank+SheafGraph rho maps. No API contract validation | crates/prime-radiant/tests/ruvllm_integration_tests.rs | R139 | Open |
| H259 | **Unsafe pointer cast UB in test** — e2e_integration_test.rs L840 uses Arc::as_ptr() as *mut MockLlmBackend violating Rust aliasing rules. Works only because MockLlmBackend.load_model sets AtomicBool, but pattern is unsound | crates/ruvllm/tests/e2e_integration_test.rs:840 | R139 | Open |
| H260 | **e2e_integration_test.rs does NOT test cross-crate integration** — "E2E" label is misleading. No hnsw_router, no claude_flow module, no NAPI bridge, no SIMD paths. Only tests ruvllm-internal modules with mock backend | crates/ruvllm/tests/e2e_integration_test.rs | R139 | Open |
| H261 | **3 #[ignore] tests don't run real inference** — test_real_model_generation only reads GGUF magic bytes, test_real_model_streaming checks env var is non-empty, test_real_model_quantization does memory math. Even with GGUF model present, no actual inference occurs | crates/ruvllm/tests/e2e_integration_test.rs:1257-1329 | R139 | Open |
| H256 | **Zero relationship between setup.ts and memory-initializer.ts** — completely independent subsystems. Setup generates Docker+SQL scaffolding; memory-initializer configures in-process backends. Neither references the other. Two parallel PostgreSQL bootstrapping paths with no integration | v3/@claude-flow/cli/src/commands/ruvector/setup.ts | R138 | Open |
| H262 | **ruvector core barrel exports ALL 23 modules including dead code** — npm/packages/ruvector/src/core/index.ts re-exports everything with no selective gating. sona-wrapper (62-68% dead per R137) and other broken modules leak to consumers via `import from '@ruvector/core'` | npm/packages/ruvector/src/core/index.ts | R139 | Open |
| H263 | **build-native.yml test step uses continue-on-error:true** — native module smoke test (load .node, check exports) failures cannot block the build. Pattern consistent with CI facade finding. 5-platform binaries published regardless of test outcome | .github/workflows/build-native.yml:151 | R139 | Open |
| H264 | **sona-napi.yml publish failures silently swallowed** — `npm publish ... \|\| echo "Warning: may already exist"` makes real publish errors indistinguishable from already-published packages. All 7 platform packages + main @ruvector/sona use this pattern | .github/workflows/sona-napi.yml:227 | R139 | Open |
